# 250307

## Variadic arguments

C, C++에서는 함수에 가변인자를 사용할 수 있다. 아래와 같은 스타일로 사용한다.

```c
#include <stdio.h>
#include <stdarg.h>

void printAddresses(int count, ...) {
    va_list args;
    va_start(args, count);

    for (int i = 0; i < count; i++) {
        int* argPtr = va_arg(args, int*);  // 정수형 변수 주소 가져오기
        printf("Argument %d address: %p\n", i, (void*)argPtr);
    }

    va_end(args);
}

int main() {
    int a = 10, b = 20, c = 30;
    printAddresses(3, &a, &b, &c);
    return 0;
}

```

va_start로 시작 인자의 주소값을 저장하고

va_arg로 현재 인자 주소 반환하고 다음 인자로 넘어간다.

va_end로 가변 처리를 종료한다.

## 인자의 메모리 공간

64bit os에서는 8byte 씩 떨어져서 저장한다. 일반적으로 연속적으로 저장되지만 보장이 되지는 않는다. 다만 …을 이용한다면 연속적으로 저장되는 것이 보장된다. 변수 형식에 따라 간격이 8byte가 아닐수도 있지만 이전 인자 다음 공간에 있는 것은 보장된다.

## Tail Call Optimaization

서브루틴 체인이 일어날 때 각 서브루틴의 return에 함수를 호출하는 것
