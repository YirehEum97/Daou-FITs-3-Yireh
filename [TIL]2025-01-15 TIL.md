# 250115

# SELECT 기초

## ORDER BY

SQL에서 데이터를 정렬하는 데 사용되는 절

기본적으로 오름차순으로 정렬. 내림차순으로 정렬하고 싶으면 DESC 키워드 사용

하나 이상의 컬럼을 기준으로 정렬 가능

### 다중 컬럼 정렬

앞에 있는 컬럼이 우선순위가 있다. 해당 컬럼으로 정렬하고 같은 경우 후순위 컬럼에 따라 정렬된다.

```sql
SELECT NAME, DEPARTMENT_ID, SALARY
FROM EMPLOYEES
ORDER BY DEPARTMENT_ID ASC, SALARY DESC;
```

위의 코드의 경우 department_id가 낮은 데이터가 위로 가고 department_id가 같은 데이터들의 경우는 salary가 높은 데이터가 위로 간다.

### NULL

Oracle에서는 NULL값은 최대값으로 간주함

NULLS FIRST, NULLS LAST 등으로 NULL값의 위치를 정할 수 있다.

## 페이징

대량의 데이터를 일정한 크리로 데이터를 나누어 보여주는 방법

사용자 경험, 성능 최적화를 위해 중요

### 페이징 쿼리 기본 개념

ROWNUM : 오라클에서 결과 집합의 각 행에 부여되는 번호, 슈도 컬럼

ROW_NUMBER() 함수 : 오라클의 윈도우 함수로, 정렬 기준에 따라 고유 번호를 부여

```sql
SELECT ROWNUM, COLUMN_LIST FROM (
	SELECT column_list
	FROM table_name
	ORDER BY REG_DATE DESC
)
WHERE ROWNUM BETWEEN 1 AND 10;
```

### 쿼리 작성 시 주의사항

- 정렬 기준
    - ORDER BY는 페이징 쿼리에서 필수
    - 정렬 기준이 없으면 결과 순서가 비정상적으로 출력될 수 있음
- 성능 고려
    - 대규모 데이터셋에서 OFFSET 사용 시 성능 저하 가능
    - 인덱스를 적절히 설정하여 쿼리 성능 최적화
- 다양한 페이지 크기 테스트
    - 다양한 크기의 페이지를 고려하여 쿼리를 설계

### OFFSET-FETCH

```sql
SELECT column_list
FROM table_name
ORDER BY some_column
OFFSET :start_row -1 ROWS FETCH NEXT :page_size ROWS ONLY;
```

## DISTINCT

SQL에서 중복된 데이터를 제거하여 고유한 값만 반환하는 키워드

SELECT문과 함께 사용해 특정 컬럼의 고유한 값만 조회할 수 있음

```sql
-- 문법
SELECT DISTINCT 컬럼명
FROM 테이블명
WHERE 조건;
```

## GROUP BY

데이터베이스에서 데이터를 특정 기준을 묶는 작업

집계 함수와 함께 사용하여 그룹별 요약 정보 제공

```sql
-- 문법
SELECT 컬럼, 집계함수
FROM 테이블
GROUP BY 컬럼;
```

GROUP BY 대상이 되는 컬럼은 SELECT문에 포함되어야 한다.

GROUP BY는 SELECT보다 먼저 수행되기 때문에 ALIAS를 사용할 수 없다.

### GROUP BY 집계함수

- COUNT() : 행의 개수 반환
- SUM() : 합계 반환
- AVG() : 평균 반환
- MAX() : 최대값 반환
- MIN() : 최소값 반환

### GROUP BY와 WHERE

WHERE절은 GROUP BY 전에 조건을 필터링

급여가 5,000이상인 직원 중에서 부서별로 그루핑하는 함수

```sql
SELECT DEPARTMENT_ID, COUNT(*) AS EMP_COUNT
FROM EMPLOYEES
WHERE SALARY >= 5e3
GROUP BY DEPARTMENT_ID;
```

### GROUP BY와 HAVING

HAVING절은 GROUP BY 이후 그룹별 조건 필터링

평균 급여가 6,000 이상인 부서만 출력

```sql
SELECT DEPARTMENT_ID, AVG(SALARY) AS AVG_SALARY
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
HAVING AVG(SALARY) >=6e3;
```

GROUP BY는 무거운 연산이라 같은 결과를 낸다면 HAVING보다는 WHERE을 쓰는 것이 바람직하다.

# 함수

데이터를 처리하거나 계산하여 결과를 반환하는 사전 정의된 명령어

## 분류

### 단일 행 함수

한 행에 대해 하나의 결과를 반환

- 문자 함수 : 문자열 변환 및 처리
- 숫자 함수 : 수학적 계산
- 날짜 함수 : 날짜 및 시간 처리
- 변환 함수 : 데이터 형식 변환
- NULL 처리 함수 : NULL 값 처리

### 집계 함수

여러 행을 그룹으로 묶어 하나의 결과를 반환

- COUNT(), SUM(), AVG(), MAX() 등이 있음

## 종류

### 문자함수

문자열 변환, 검색, 길이 계산 등을 제공

- UPPER, LOWER : 대소문자 변환
- LENGTH, SUBSTR : 문자열 길이 혹은 문자열 일부 추출
- INSTR, TRIM : 특정 문자열의 첫 위치 출력, 공백 제거
- LPAD, RPAD : 문자열 채움
- REPLACE, CONCAT : 문자열 대체, 문자열 연결. CONCAT은 ||로 대체 가능

### 숫자함수

- ROUND, TRUNC : 반올림, 숫자 버리기
- ABS, MOD : 절대값, 모듈러
- CEIL, FLOOR : 올림, 내림

### 날짜함수

- SYSDATE, CURRENT_DATE : 현재 날짜와 시간을 반환. Oracle에서는 주로 SYSDATE를 씀
- ADD_MONTHS, MONTHS_BETWEEN : 현재 날짜에서 n개월 추가, 기간 사이 몇달 차이인지 계산(소수점 단위)
- NEXT_DAY, LAST_DAY : 지정된 요일의 다음 날짜와 해당 월의 마지막 날짜를 반환
- EXTRACT, TRUNC : 날짜에서 특정 요소 추출 혹은 특정 단위로 자름
    - TRUNC로 월을 표시하는 경우 일, 시, 분, 초 등 아래 단위는 기본 값으로 설정됨

### 변환 함수

데이터 타입을 다른 타입으로 변환하는 함수

- TO_CHAR : 문자열로 변환
- TO_DATE : 날짜형으로 변환
- TO_NUMBER : 숫자로 변환

### NULL 처리 함수

- NVL, NVL2 : NULL값을 대체하는 함수, NULL 여부에 따른 값 반환
- COALESCE : NULL이 아닌 첫번째 값 반환
- NULLIF : 두 값이 같으면 NULL 반환

### CASE WHEN

SQL 조건에 따라 값을 반환하는 문법

가독성이 뛰어나 복잡한 조건을 처리할 때 유용

```sql
-- 방법 1
CASE
	WHEN 조건1 THEN 결과1
	WHEN 조건2 THEN 결과2
	ELSE 결과
END

-- 방법 2
CASE 컬럼명
	WHEN 값 THEN 결과1
	WHEN 값 THEN 결과2
	ELSE 결과
END
```

### DECODE

특정 값에 따라 다른 값을 반환하는 함수

SELECT 문에서 주로 사용

CASE WHEN 보다 간단한 조건 처리에 적합

```sql
SELECT EMPLOYEE_ID,
		DECODE(JOB_ID,
			'ADMIN','관리자',
			'DEV', '개발자',
			'HR', '인사담당자',
			'기타') AS DEPARTMENT_NAME
FROM EMPLOYEES;
```

# JOIN

여러 테이블의 데이터를 결합하여 하나의 결과 집합을 생성하는 SQL 절

테이블 간 관계를 활용하여 데이터 조회

## 종류

### INNER JOIN

두 테이블 간에 일치하는 데이터만 반환

```sql
-- 방법 1
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
INNER JOIN DEPARTMENTS D
	ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
	
-- 방법 2
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID

```

### OUTER JOIN

LEFT OUTER JOIN, RIGHT OUTER JOIN, FULL OUTER JOIN이 있음

LEFT나 RIGHT 의 경우 해당 테이블이 메인이 되어 반대편 테이블의 데이터를 반환. 대부분 LEFT를 씀

일치하지 않는 경우 NULL 반환

```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
	LEFT OUTER JOIN DEPARTMENTS D
		ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
```

위의 함수의 경우 E 테이블은 그대로 있고 E와 D의 DEPARTMENT_ID가 같은 경우 D.DEPARTMENT_ID에 추가되고 E에 있지만 D에 없는 경우 NULL값이 반환된다.

FULL OUTER JOIN은 LEFT와 RIGHT 테이블의 모든 행 반환

### SELF JOIN

같은 테이블을 두 번 사용하여 JOIN 수행

### CROSS JOIN

두 테이블 간의 모든 행의 조합을 반환(카테시안 곱)

결과 집합의 크기가 매우 커질 수 있으니 조심해서 사용

INNER JOIN에서 WHERE 절을 빼먹으면 CROSS JOIN이 발생하기 때문에 조심해야함

### NATURAL JOIN

동일한 이름의 컬럼을 기준으로 JOIN

조건을 명시하지 않아도 자동으로 연결

```sql
SELECT NAME, DEPARTMENT_NAME
FROM EMPLOYEES
NATURAL JOIN DEPARTMENTS;
```

### 유의사항

- JOIN 조건 누락
JOIN 조건이 없으면 CROSS JOIN이 되어 매우 큰 크기의 연산이 발생할 수 있음
- NULL 처리
OUTER JOIN 시 NULL 값을 처리하는 로직 필요
- 성능
큰 테이블 간 JOIN은 성능 저하 가능, 적절한 인덱스 설정 필요