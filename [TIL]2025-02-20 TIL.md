# 250220

# 실습

### session에 있는 id를 불러오는 과정

session에 있는 member라는 변수를 MemberVO의 instance로 불러온다. 이후 getId() method를 이용해 불러오면 된다.

request 단에 “id”로 명시하고 추가하는 방법도 있는데 어떤 것이 서비스에 더 이득인지 잘 모르겠다.

```java
HttpSession session = request.getSession();
MemberVO member = (MemberVO) session.getAttribute("member");
String author = member.getId();
```

### 특정 페이지로 넘어가는 방법

html에서 하이퍼링크로 넘어가는 것 말고 servlet에서 특정 함수가 동작하면서 자동으로 사용자의 페이지를 이동시키는 방법이다. 글 작성 페이지에서 등록을 클릭하면 자동으로 글 목록으로 이동하도록 함수 가장 마지막에 해당 함수를 넣었다.

```java
response.sendRedirect(request.getContextPath() + "/bulletin");
```

### VO와 DB의 괴리

VO와 DB는 무조건 1대1로 대응한다고 생각했다.

게시판에서 댓글수와 좋아요 수를 표시하는 과정에서 VO와 DB를 1대1로 관리하지 않는 것이 더 이득인 경우도 있다는 것을 알게 되었다.

likeCount, commentCount의 경우 DB에는 존재하지 않는다. 각각 LIKES, COMMENTS라는 테이블에서 post의 id와 같은 값을 count한 결과물이다.

정규화를 위해 차마 DB에 넣지는 못하겠어서 불러올 때마다 request에서 id를 가져오고 DB에서 count를 하는 방식으로 구현했다. 그랬더니 세션에 너무 자주 사용되는 문제가 발생했다.

따라서 처음에 게시판을 불러올 때 서브쿼리를 이용해서 한번에 불러오는 방법을 채택했다.

이때 결과는 VO 형식으로 받는데 VO에 서브쿼리의 결과물에 해당하는 필드가 없다면 오류가 발생한다. 따라서 DB에는 없지만 VO에 있는 필드가 두 개 생겼다.

```java
public class BoardPostVO {
    private int id;
    private String title;
    private String content;
    private String author;
    private Timestamp regDate;
    private int likeCount;
    private int viewCount;
    private int commentCount;
    
    ...
}
```

### 댓글 관리하기

댓글DB에 pk를 뭐로 정할까 고민하다가 게시글id에 sequence를 추가하는 것으로 했다.

물론 댓글id를 따로 만들어서 관리해도 되지만 굳이 서로 다른 게시물의 댓글이 distinct한 id를 부여해야하는 이유를 모르겠고 그렇게 된다면 모든 게시물이 하나의 uuid 생성기를 거쳐야해서 병목이 발생할 수 있다고 판단했다.

게시글의 순번은 DB에 시퀸스를 만들어서 관리했다. 댓글의 경우 게시물 별로 시퀸스를 생성하고 사용하는 것은 시퀸스 생성으로 공간이 많이 필요할 것이라고 판단했다.

따라서 게시물id로 댓글을 조회하고 sequence의 최대값 + 1로 다음 sequence를 부여했다.

다만 여러 계정에서 동시에 댓글을 작성한다면 같은 sequence를 갖게 되어 pk가 중복되는 문제가 발생할 수 있을 것 같지만 확실하진 않으니 좀 확인해봐야겠다.

```xml
<insert id="insertComment" parameterType="Comment">
        <![CDATA[
        INSERT INTO COMMENTS (bid, sequence, user_id, ccontent)
        VALUES (#{id},
                (select nvl(max(sequence), 0) + 1 from COMMENTS where bid = #{id}),
                #{author},
                #{content})
        ]]>
    </insert>
```