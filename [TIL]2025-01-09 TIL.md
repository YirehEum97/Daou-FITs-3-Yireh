# 250109

# 인공지능

인간의 지각, 학습, 추론 능력을 인공적으로 구현한 기술

룰베이스 알고리즘, 머신러닝, 딥러닝을 모두 포함

### 튜링 테스트

컴퓨터와 사람이 기계어로 대화를 하고 심판인 사람이 어느 쪽이 사람인지 구분을 함.

## 머신러닝

사람의 개입 없이 자동으로 기계가 학습하게 함

### 머신러닝 알고리즘의 분류

- 지도학습 : 데이터에 라벨링이 되어 있어 데이터에서 답을 찾는 패턴을 파악해 이를 토대로 답을 유추. 분류, 회귀 모델이 포함됨
- 비지도학습 : 라벨링이 없는 데이터에서 데이터의 패턴, 데이터간 유사도 등을 학습함. 비슷한 데이터를 묶는 클러스터링 등이 포함됨
- 강화학습 : 에이전트의 행동에 대한 보상을 주며 이를 바탕으로 최적의 방식을 학습

### 분류 모델

출력으로 나올 수 있는 값이 이산적인 모델

금융에서 발생할 수 있는 주요 분류 문제 : 사기 탐지, 대출 승인 여부, 매도 매수 결정, 이탈 여부, 금융 상품 추천

### 분류 알고리즘 종류

- 의사 결정 트리
- 랜덤 포레스트
- LDA(선형판별분석)
- KNN
- SVM
- 신경망(NN)
- 부스팅

### 회귀 모델

출력으로 나올 수 있는 값이 연속적인 모델

### 성능 평가

혼동 행렬, accuracy, precision, recall, f1-score 등 다양한 지표로 모델을 평가한다.

### 하이퍼 파라미터 튜닝

직접 수정할 수 있는 파라미터를 하이퍼 파라미터라고 한다. 랜덤 포레스트의 경우 모델 깊이, 트리 추출 획수(n_estimators) 등이 포함된다.

## 딥러닝

머신러닝의 하위 분류로 여러 층의 인공신경망을 활용해 복잡한 데이터에서 특징을 자동으로 학습

### CNN

합성곱(convolution)을 사용해 데이터의 패턴을 파악하는 모델

## 생성형 AI

새로운 데이터, 이미지, 음성, 텍스트 등을 생성하는 기술

ChatGPT, Gemini 등 다양한 서비스가 있다.

### 인코더, 디코더

인코더 : 데이터를 하나의 벡터(context vector)로 압축

디코더 : 벡터를 사용해 데이터를 만듦

### 트랜스포머

기존의 인코더-디코더를 따르면서 어텐션 만으로 구현한 모델

어텐션 : 입력 시퀴스 각 요소의 중요도를 측정하기 위해 소프트맥스 함수를 사용해 어텐션 가중치를 계산. 입력과 가중치를 활용해 출력을 생성

### LLM

다양한 자연어처리를 할 수 있는 거대한 모델

### Prompt Engineering

LLM 모델에서 더 양질의 답변을 얻기 위해 입력값인 prompt를 짜는 방법

### RAG

한계점 : 훈련한 적 없는 영역에서 성능이 떨어짐. 정확한 값을 반환하기 어려움. 예를 들어 엔비디아의 새 제품의 가격을 예측할 수 있지만 정확한 가격을 알려줄 수 없음

해결 방안 : 답변에 필요한 정보를 담은 DB를 연결하여 정확한 수치를 바탕으로 답변을 생성함