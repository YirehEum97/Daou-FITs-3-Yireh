# 250207

# 주요 class

## String

특징

- immutable하다. 데이터가 변경되면 객체를 수정하는 것이 아니라 새로운 주소에 객체를 생성한다.
- String 객체는 Heap 영역 안의 String pool에서 관리된다. new 키워드로 생성하는 것이 아니라 “”를 이용해 생성하는 경우 String pool에서 먼저 객체를 검색하고 없다면 생성, 있다면 해당 객체의 주소값을 저장한다.
- final class라서 하위 클래스를 생성하지 못한다.

## Array

특징

- 같은 type의 data를 여러개 저장할 수 있는 자료구조
- 배열은 객체
- 배열을 만들기 위해서 new keyword 사용
- mutable하다.

```java
int[] lottoNumbers = new int[6];
String[] names = new String[10];
```

## ArrayList

list 계열에서 대표적인 class

Array와 차이점

- 크기가 유동적
- 다른 Data Type을 저장할 수 있음
- instance만 저장할 수 있음
    - int 대신 Integer를 쓰는 등 primitive type을 instance로 바꿔야한다.
- generic을 이용해서 같은 데이터 타입으로 고정할 수 있음
    - 일반적으로 interface를 타입으로 정해 프로그램 사용성을 높임

```java
ArrayList<Integer> list = new ArrayList();
ArrayList<Serializable> list = new ArrayList();
```

## HashMap

특징

- (key, value) 쌍으로 이루어진 data를 저장할 수 있는 자료구조
- key가 hashing 되어 접근 속도가 매우 빠르다
- 일반적으로 순서가 존재하지 않지만 최근에 순서 개념이 도입된 map도 추가되었다.
- put() 메소드로 데이터를 추가한다.
- generic 사용 가능

```java
import java.util.HashMap;
import java.util.Map;

class Student {
	
}

class MyTest {

	public static void main(String[] args) {

		Map<String,String> map = new HashMap<String,String>();
		map.put("1", "홍길동");
		map.put("2", "최길동");
		
		System.out.println(map.get("2")); // 최길동
		
		for (String key : map.keySet() ) {
			System.out.println(key + " - " + map.get(key));        
		}
	}

}
```

## HashSet

특징 

- 데이터가 hashing 되어 저장된 자료구조
- add() 메소드를 이용해 데이터 추가
- generic 사용 가능
- Iterator class를 이용해  iterator 객체를 생성하면 next() 메소드로 요소를 순서대로 접근할 수 있다.
    - hasNext() 메소드로 아직 접근하지 않은 객체가 남아있는지 확인할 수 있다.
- 내부적으로 HashMap을 이용
    - (key, 더미데이터)로 이루어져있음
    - 그러나 상속 관계는 아님

## Exception

특징

- 특정 상황이 발생하면 JVM에서 자동으로 Exception class의 instance를 생성
- exception handling : 예외 상황으로 인해 프로그램이 비정상 종료되는 것을 방지하기 위해 예외 상황에서의 동작을 지정해 지속적으로 동작할 수 있도록 하는 것

```java
// case1
public class ExceptionTest {
    public static void main(String[] args) {
        int a = 10 / 0; 
    }
}
// Exception in thread "main" java.lang.ArithmeticException: / by zero

// case2
public class ExceptionTest {
    public static void main(String[] args) {
        try {
            int result = 10 / 0;
        } catch (ArithmeticException a){
            System.out.println("예외가 발생했어요");
        }
    }
}
// 예외가 발생했어요

// case3
public class ExceptionTest {
    public static void main(String[] args) {
        try {
            int result = 10 / 0;
        } catch (Exception e) {
            System.out.println("예외 발생1");
        } catch (ArithmeticException a){
            System.out.println("예외 발생2");
        }
    }
}
// java: exception java.lang.ArithmeticException has already been caught
```

0으로 나누는 것은 허용되지 않기 때문에 프로그램이 Exception class 중 ArithmeticException의 객체를 생성하고 프로그램이 종료된다.

catch 구문에 ArithmeticException의 instance를 넣으면 JVM은 프로그램을 종료하는 대신 catch 구문을 실행한다.

ArithmeticException보다 Exception이 더 넓은 범위의 오류코드를 받는다. case3의 경우 이미 오류코드가 Exception에서 반환됐기 때문에 ArithmeticException에서 오류가 발생한다. 여러 catch문을 쓸거면 점점 넓은 오류코드를 쓰거나 서로 같은 레벨의 오류코드를 작성해야한다.