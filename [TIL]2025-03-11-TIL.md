## Undefined Behavior(UB)

C 언어에서는 한 표현식에서 같은 변수를 여러 번 수정하는 경우 결과가 정의되지 않음

즉 컴파일러마다 결과가 동일하지 않을 수 있음

아래는 내 노트북에서 코드를 실행한 결과다.

```c
for (int i = 0; i < 4; i++) { 
    printf("%d %d\n", i, ++i);
}
// 1 1
// 3 3

for (int i = 0; i < 4; i++) { 
    printf("%d %d\n", i, i++);
}
// 1 0
// 3 2
```

첫번째 코드의 경우는 0 1 2 3이 출력될 것을 기대했지만 i가 출력되기 전 전위연산자가 먼저 실행되면서 1 1이 출력되었다.

두번째 코드의 경우 0 0 2 2를 기대했지만 1 0 3 2가 출력되었다.

후위연산자가 먼저 실행되면서 i의 값이 1로 바뀌었고 i++을 출력할 때는 기존의 i값을 잠시 저장한 temp에서 0을 가져와 출력하기 때문에 이와 같은 일이 발생한다.

## 포인터에 자료형이 있는 이유

이 부분은 아직 정확히 몰라 이후에 찾아볼 예정

어떤 자료형이 오던간에 포인터의 크기는 os에서 정해진 크기를 따르며 주소값을 저장한다는 공통점을 가지고 있다. 그러면 포인터는 굳이 자료형을 가질 필요가 없을 것 같은데 int형 변수의 포인터는 int *로 고정되어있다. char *에 저장하려고 했으나 예상과 다른 결과가 출력되었다. 포인터를 저장하면 해당 공간에 주소값 외에도 다양한 정보가 저장되는 것은 아닐까 예상중이다.

## 다중 포인터

배열 문자열 등 이미 포인터가 내부적으로 사용되는 자료형을 가리키는 포인터가 필요하다. 그래서 포인터의 포인터인 다중 포인터가 생겨났다.

## 음수 인덱스

Python에서는 인덱스에 음수가 들어갈 경우 자료의 마지막부터 검색을 한다.

C에서는 인덱스에 음수가 들어간 경우 메모리 공간에서 해당 칸만큼 이전의 값을 반환한다.

따라서 1차원 배열에서는 트래시값이 반환된다.

2차원 이상부터는 유의미하게 사용할 수 있다.

```c
int main() {
	int a[2][4] = { {1,2,3,4},{5,6,7,8} };
	printf("%d", a[1][-1]); // 4
	return 0;
}
```

두번째 줄의 -1번째 메모리 공간에 접근하면 첫번째 줄의 마지막 메모리 공간에 접근하여 4를 반환한다. 

놀랍게도 존재하지도 않는 세번째 줄의 -1번째 메모리 공간에 접근하면 두번째 줄의 마지막 메모리 공간에 접근하여 8을 반환한다.
