## 구조체

서로 다른 형태의 데이터를 여러 개 모아서 하나의 자료구조로 표현

구조체로 만들어진 자료는 연속된 메모리 공간에 할당됨

### typedef

구조체의 이름을 정의하는 것

alias라고 보면 된다.

## 예시

book이라는 struct를 만들고 등록과 검색을 진행한다.

```c
void f1002() {
	struct book {
		char title[50];
		char author[50];
		int publish;
		char genre[50];
	};
	int choice, count=0;
	struct book books[10];
	while (1) {
		printf("--------------------도서 관리 프로그램--------------------\n");
		printf("메뉴를 선택하시오\n1. 도서 등록\n2. 도서 검색\n3. 종료하기\n");
		scanf("%d", &choice);
		getchar();
		if (choice == 1) {
			printf("--------------------도서 등록--------------------\n");
			printf("도서 제목을 입력하세요: ");
			gets(books[count].title);
			printf("\n도서 저자를 입력하세요: ");
			gets(books[count].author);
			printf("\n도서 출판년도를 입력하세요: ");
			scanf("%d", &books[count].publish);
			getchar();
			printf("\n도서 장르를 입력하세요: ");
			gets(books[count].genre);
			count++;
		}
		else if (choice == 2) {
			char search[50];
			printf("--------------------도서 검색--------------------\n");
			printf("검색할 도서의 장르를 입력하세요: ");
			gets(search);
			for (int i = 0; i < 10; i++) {
				if (strcmp(search, books[i].genre) == 0) {
					printf("도서 제목: %s\n", books[i].title);
					printf("도서 저자: %s\n", books[i].author);
					printf("도서 출판년도: %d\n", books[i].publish);
				}
			}
		}
		else {
			break;
		}
	}
}
```

## 예시2

student라는 struct를 만들고 입력을 받아 저장한다.

```c
void f1001() {
	struct student {
		char name[10];
		int stid;
		int sub1, sub2, sub3;
	};

	struct student students[3];
	for (int i = 0; i < 3; i++) {
		printf("학생의 이름을 입력하세요: ");
		scanf("%s", students[i].name);
		printf("학생의 학번을 입력하세요: ");
		scanf("%d", &students[i].stid);
		printf("학생의 과목1 점수를 입력하세요: ");
		scanf("%d", &students[i].sub1);
		printf("학생의 과목2 점수를 입력하세요: ");
		scanf("%d", &students[i].sub2);
		printf("학생의 과목3 점수를 입력하세요: ");
		scanf("%d", &students[i].sub3);
	}

	printf("학생 성적 정보\n");
	for (int i = 0; i < 3; i++) {
		printf("이름 : %s, 학번 : %d, 평균	: %.2f\n", students[i].name, students[i].stid, (students[i].sub1 + students[i].sub2 + students[i].sub3) / 3.0);
	}
}
```
