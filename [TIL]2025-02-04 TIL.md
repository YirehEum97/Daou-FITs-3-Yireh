# 250204

# JAVA

### 언어 특징

- 일반적으로 하이레벨 랭귀지로 작성한 코드는 컴파일러를 통해 머신코드로 바뀜
- 자바는 컴파일러를 통해서 바이트코드로 변경
- 바이트코드를 JVM이 읽어들여서 머신코드로 변경해서 실행

.java(하이랭귀지) → (javac compiler) → .class(바이트코드) → JVM

- 객체지향 언어
- 네이밍 컨벤션
    - 클래스는 pascal case(첫글자 대문자, MyFriend)를 따름
    - 변수명은 camel case(두번째 단어 대문자, myFriend)
- 하나의 프로그램에 수많은 클래스가 존재함
    - 효율적인 관리를 위해 package로 클래스를 묶어 관리함
    - package는 폴더로 class는 파일로 관리됨
    - class 작성 시 package를 명시하지 않으면 ‘unnamed_package’라는 package 안에 포함됨

### 객체지향

- 유지보수를 위해 만들어진 패러다임. 개발할 때 유지보수를 염두에 두고 해야한다.
- 해결해야하는 문제를 구성하는 구성요소를 파악 후 각 요소를 프로그래밍
- 추상화를 거쳐 나온 여러가지 정보를 상태와 행위로 구분
    - 상태는 변수(필드), 행위는 함수(메소드)로 표현
    - 필드와 메소드를 묶어 클래스로 표현
- 코드로 표현할 때 클래스 1개당 파일 1개를 매핑
    - 클래스의 이름을 파일 이름으로 명명(퍼블릭 클래스의 경우)
- 클래스 자체는 메모리 공간을 가지고 있는 것이 아님
    - 클래스로부터 저장공간을 생성함. 해당 저장공간을 instance라고 부름
    - 클래스를 결국 데이터 타입의 역할을 수행. 고정된 타입이 아니기 때문에 추상 데이터 타입(ADT: Abstract Data Type)이라고 함
    - 

### 구성요소

- JVM(JAVA Vertual Machine)
    - bytecode를 읽어들여서 실행하는 실행 주체
- JRE(JAVA Runtime Enironment)
    - 자바 실행 환경
    - JVM + Java Class Library
- JDK(Java Development Kit)
    - 자바개발도구
    - JRE + 자바개발도구(javac, java, javadoc, etc…)

### 데이터 타입

- Strong type 언어로 변수를 지정할 때 미리 타입을 선언해야한다.
- Java는 대소문자를 구분하고 변수길이의 제한이 없다
- 예약어로는 변수를 만들 수 없다
- 숫자로 시작하는 변수 불가능
- 특수문자는 _와$만 허용
- 데이터 타입은 두가지로 나뉨
    - primitive data type
        - 해당 타입으로 선언된 변수 안에 직접 데이터가 저장
        - 8가지 타입이 존재. 메인 타입은 볼드체.
            - 정수형 : byte, short, **int,** long
            - 실수형 ; float, **double**
            - 문자형 : char
            - 논리형 : boolean
    - reference data type
        - class로 선언된 변수를 지칭
        - 이 타입으로 선언된 변수 안에는 메모리 주소가 저장

### 상수

- 상수 선언 가능(final 키워드 사용)
- final int MAX_SPEED = 100;
    - 상수는 변수를 대문자로 작성
    - 선언과 동시에 초기화가 이루어져야함

### 형변환

- 묵시적 형변환
    - 지정하지 않았는데 자동으로 형변환이 되는 경우
    - ex) int + double → double + double
- 명시적 형변환
    - (타입)피연산자 형식으로 바꾸려는 형식을 명시함

### 예시

```java
class Customer {

	// fields
	String name; // 고객이름
	int age;     // 고객나이
	int height   // 고객키
	
	
	// methods
}
```

### JVM의 메모리 영역

- Method Area
    - class의 변수와 함수
        - 실제 메모리 공간이 아니라 변수와 함수에 대한 정보 저장
- Heap
    - instance가 위치
- Stack
    - 변수 값이 저장된 공간

### Overloading

같은 이름이나 같은 형태로 여러 기능을 수행하는 경우

예시

```java
5+10 // -> 15 숫자 더하기

"안녕" + "하세요" // "안녕하세요" 문자열 합치기
```