# 250107

# 자료구조

프로그램에서 쉽게 이용할 수 있도록 구성된 데이터 간의 논리적인 관계

종류 : 배열, 리스트, 스택, 큐, 그래프, 트리

### 배열

같은 자료형 데이터를 순서대로 나열한 구조

메모리 공간에서 자료가 순서대로 저장되기 때문에 첫 요소와 인덱스를 알고 있으면 바로 데이터에 접근이 가능하다.

데이터가 순서대로 있어야하기 때문에 삽입, 삭제를 하는 경우 데이터를 빈칸 없이 이동시키는 과정이 필요하다.

### 리스트

각 데이터들을 포인터로 연결하여 관리하는 구조

메모리가 연속적으로 저장될 필요가 없고 데이터 영역에 여러개 데이터가 저장될 수 있음

### 스택

데이터의 삽입과 삭제가 한쪽 방향에서만 일어나는 구조

가장 나중에 삽입된 데이터가 가장 먼저 삭제된다

### 큐

데이터가 한쪽에서 삽입이 되고 반대 방향에서 삭제가 되는 구조

가장 먼저 삽입된 데이터가 가장 먼저 삭제된다

### 그래프

정점과 간선으로 구성된 자료구조

정점 : 위치 혹은 데이터

간선 : 정점과 정점을 연결하는 선

그래프 종류 :  무방향 그래프, 방향 그래프, 완전 그래프, 가중 그래프

인접행렬 : 그래프를 행렬로 나타내는 방법 중 하나로 행과 열이 정점에 해당하고 간선 정보를 저장한다.

근접행렬 : 그래프를 행렬로 나타내는 방법 중 하나로 행과 열은 각각 정점과 간선을 의미한다. 정점과 간선의 관계를 값으로 저장한다.

### 트리

그래프 중 아래 조건을 만족하는 그래프이다.

- 하나의 루트 노드를 가진다.
- 각 노드는 0개 이상의 자식 노드를 가진다.
- 각 노드는 하나 혹은 0개의 부모 노드를 가진다.

루트노드부터 시작해서 임의의 노드까지 방문한 노드의 수는 레벨이라고 부른다.

트리의 종류

- 이진 트리 : 모든 노드의 자식 노드가 2개 이하인 트리
- 완전 이진 트리 : 단말 노드를 제외한 모든 노드가 2개의 자식 노드를 가지고 있는 트리
- 포화 이진 트리 : 완전 이진 트리 중에서 모든 노드가 채워진 이진 트리. 즉 모든 단말 노드의 레벨이 같은 완전 이진 트리.
- 이진 탐색 트리 : 이진 트리 중에서 모든 노드가 서로 다른 데이터를 가지고 있으며 왼쪽 자식 노드는 부모 노드보다 작고 오른쪽 자식 노드는 부모노드보다 크도록 데이터를 삽입
- AVL 트리 : 이진 탐색 트리 중 특정 방향으로 편향이 생기지 않게 유지를 한다. 균형을 확인하고 균형이 맞지 않으면 회전을 통해 균형을 맞춤
- 레드블랙 트리 : 이진 탐색 트리 중 하나로 AVL 트리처럼 균형을 유지함. 균형 확인을 위해 노드에 색을 부여해 관리함

### 힙

완전 이진트리를 기반으로 하는 자료구조

삽입, 삭제, 검색이 용이함.

order가 있는 데이터에 사용할 수 있음

최대 힙은 모든 노드가 본인의 자식 노드보다 값이 크다. 즉 루트 노드가 항상 가장 큰 값으로 고정된다. 최소 힙은 순서가 반대로 루트 노드가 가장 작은 값으로 고정된다.

### 해시테이블

key값을 이용해서 데이터를 빠르게 검색할 수 있도록 설계된 자료구조

key값에 대한 해시함수 반환값을 인덱스로 데이터를 저장하고 검색함.

충돌이 일어나지 않는다면 평균적으로 O(1) 시간 복잡도로 검색, 삽입, 삭제가 가능

# 알고리즘

어떤 문제를 해결하기 위해 구성된 일련의 절차

### 시간복잡도

알고리즘이 얼마나 효율적으로 동작하는지 평가할 수 있는 중요한 척도

오메가 표기법 : 최선의 경우에서의 시간 복잡도

세타 표기법 : 평균적인 경우에서의 시간 복잡도

빅오 표기법 : 최악의 경우에서의 시간 복잡도

일반적으로 빅오 표기법을 사용함

### 탐색 알고리즘

- 깊이 우선탐색(DFS)

시작 정점부터 시작해서 연결된 정점을 순회하며 탐색을 한다. 이때 가장 최근에 발견된 정점을 먼저 탐색하여 깊이가 깊어지는 방향으로 탐색하게 된다.

- 너비 우선 탐색

시작 정점부터 시작해서 연결된 정점을 순회하며 탐색을 한다. 이때 가장 먼저 발견된 정점을 먼저 탐색하여 깊이가 낮은 것부터 탐색하게 된다.

- 이진 트리 순회

이진 트리에서 노드를 탐색하는 알고리즘으로 탐색 순서에 따라 나뉜다.

전위 순회(pre-order) : 루트→왼쪽→오른쪽

중위 순회(in-order) : 왼쪽 → 루트 → 오른쪽

후위 순회(post-order) : 왼쪽 → 오른쪽→ 루트

- 몬테카를로 트리 탐색

기대값이 가장 높은 트리를 확장하는 방식으로 탐색하는 알고리즘. 주로 게임 분야에서 사용된다.

- 선형 탐색

주어진 데이터 집합에서 원하는 데이터를 처음부터 순차적으로 비교하며 찾는 방법

- 이진 탐색

정렬된 데이터 집합을 반으로 쪼개가며 탐색하는 방법

### 정렬 알고리즘

- 선택 정렬 : 정렬되지 않은 부분에서 가장 작은 데이터를 찾아 가장 앞의 데이터와 교환해가는 방식
- 삽입 정렬 : 정렬된 부분에 데이터를 삽입해가며 정렬하는 방식
- 버블 정렬 : 서로 이웃한 데이터를 비교해 큰 데이터를 뒤가 오도록 교환하며 정렬하는 방식
- 퀵 정렬 : 기준키를 중심으로 작은 키는 왼쪽 큰 키는 오른쪽으로 보내는 것을 반복해 정렬하는 방식
- 병합 정렬 : 재귀적으로 반으로 나누었다가 합치면서 정렬하는 방식
- 힙 정렬 : 힙에 넣었다 빼면서 정렬하는 방식

### 알고리즘 예시

재귀 알고리즘 : 자기 자신을 호출하는 방식으로 문제를 해결하는 방식.

그리디 알고리즘 : 매 순간 최적의 선택만 하는 방법으로 문제를 해결하는 방식. 해당 해가 최적이 아닐수도 있다.

최소 신장 트리 : 트리에서 일부 간선만 선택해 모든 노드를 연결하면서 간선의 가중치 합이 가장 작은 트리를 만드는 문제

다익스트라 알고리즘 : 한 노드에서 다른 노드까지의 최단 경로를 찾는 알고리즘

동적 프로그래밍 : 복잡한 문제를 작은 부분으로 나누어 해결하고 결과를 저장하여 중복으로 계산하지 않도록 하는 설계 기법