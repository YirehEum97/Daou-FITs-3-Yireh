# 250217

# Servlet

### 특징

- Servlet은 interface
- Servlet - GenericServlet class - HttpServlet class 가 있음
- 상속되는 method
    - init() : 서블릿 객체가 생성될 때 호출되는 초기화를 담당하는 메소드. 생성자 대신 init()을 이용해 초기화 진행
    - service() : 클라이언트가 서블릿을 실행할 때 호출되는 method. 이 service가 하는 일은 이미 구현되어 있어 일반적으로 overriding을 하지 않음. 클라이언트의 요청 방식에 따라 자동으로 적절한 method를 호출하는 역할
    - doGet(), doPost() : 클라이언트 요청을 받아 처리하는 method. overriding해서 작업을 하면 됨
    - destroy() : servlet instance가 WAS에서 삭제될 때 수행되는 method

### Get 방식 예시

- sevlet에 논리적인 이름을 하나 부여해 클라이언트가 해당 이름을 통해 servlet을 호출 할 수 있도록 함
    - xml을 이용해 해당 설정을 잡으면 전체 이름 관리를 하나의 파일에서 할 수 있어 관리가 편함
    - annotation을 이용해서 url 매핑하면 파일을 따로 관리하지 않고 코드로 관리 가능
- doGet()을 overriding해서 사용
    - 데이터 전달 통로를 열어 결과 데이터를 전달
    - prinWriter를 이용해서 클라이언트에게 데이터 통로를 열고 해당 통로로 데이터 전달
- 문자열을 보낼 때 다 보내고나면 명시적으로 끝났다는 것을 알려주는 것이 좋음. flush()를 통해 문자열이 끝난 것을 알려주고 out()을 통해 끝낸다.

```java
package org.example.helloservletproject;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.io.PrintWriter;

@WebServlet(value = "/myServlet")
public class MyFirstServlet extends HttpServlet {

    public MyFirstServlet(){
        System.out.println("constructor called");
    }

    @Override
    public void init() throws ServletException {
        super.init();
        System.out.println("init called");
    }

    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        resp.setContentType("text/html; charset=utf-8");
        PrintWriter out = resp.getWriter();
        out.println("<html>");
        out.println("<head></head>");
        out.println("<body>success</body>");
        out.println("</html>");
        out.flush();
        out.close();

        System.out.println("Get called");
    }
}
```

### Get, Post

- Get
    - Query String을 이용해서 데이터를 전달하는 방식
        - ?변수=값&변수=값&변수=값 →?name=홍길동&address=서울
    - getParameter(”변수명”)을 통해 전달된 데이터를 수신
    - 보안성이 전혀 없음
    - 길이 제한이 있음
- Post
    - 상대적으로 보안 가능
    - request header에 포함되어 데이터가 전달되기 때문에 길이의 제한이 없음
    - HTML 특수 태그를 사용해 서버단 프로그램 호출하는 방식