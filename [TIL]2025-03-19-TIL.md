# 최적화

## 메모리 최적화

최소한의 메모리로 데이터를 저장해 공간을 절약하는 것

### 비트 필드

구조체 내에서 변수를 비트 단위로 저장할 수 있도록 해주는 기능

```c
typedef struct {
 unsigned int is_active : 1; // 활성화 여부 (1비트)
 unsigned int is_vip : 1; // VIP 여부 (1비트)
 unsigned int has_loan : 1; // 대출 여부 (1비트)
 unsigned int over_protection : 1;// 예금 보호 한도 초과 여부 (1비트)
 unsigned int balance_status : 3; // 잔액 상태 (3비트)
} AccountStatus;
```

위와 같이 정의하면 각각의 값을 1~3개 비트를 활용해 저장한다.

unsigned int가 4byte이므로 32개 비트가 있고 예시에서는 그 중 7개 비트를 사용하고 있다. 나머지 25개 비트는 사용하지 않는다. 따라서 구조체 크기는 4바이트이다.

### 비트연산

직접 비트연산을 해 메모리 낭비를 줄이고 성능을 향상시킬 수 있음

```c
typedef struct {
 uint8_t flags; // 1바이트(8비트)로 상태 저장
} AccountStatus;
// 비트 마스크 정의
#define ACTIVE_MASK 0x01 // 0000 0001 (1비트)
#define VIP_MASK 0x02 // 0000 0010 (2비트)
#define LOAN_MASK 0x04 // 0000 0100 (3비트)
#define OVER_PROTECT_MASK 0x08 // 0000 1000 (4비트)
#define BALANCE_MASK 0x70 // 0111 0000 (5~7비트)
```

위와 같이 정의하면 구조체는 1바이트 크기를 갖는다. 그중 7개 비트를 정보를 저장하는데 쓰고 있다.

```c
void setAccountStatus(AccountStatus* account, int is_active, int is_vip, int has_loan, int
over_protection, int balance_status) {
 account->flags = (is_active ? ACTIVE_MASK : 0) |
 (is_vip ? VIP_MASK : 0) |
 (has_loan ? LOAN_MASK : 0) |
 (over_protection ? OVER_PROTECT_MASK : 0) |
 ((balance_status << BALANCE_SHIFT) & BALANCE_MASK);
}
```

위와 같이 비트연산을 통해 데이터를 저장한다면 매우 빠르게 데이터를 저장할 수 있다.

구조체를 연달아 저장하게 된다면 비트연산을 이용하는 것이 공간적으로 이득이다. 다만 일반적으로 cpu는 4바이트 혹은 8바이트 단위로 연산을 하기 때문에 연산 속도는 크게 차이가 나지 않는다.

# 파일

c언어를 이용해 파일을 읽기, 쓰기, 수정 등을 할 수 있다.

```c
FILE *fp = fopen("file.txt", "r");
 char ch = fgetc(fp);
 fclose(fp);
```

위와 같이 파일에 직접 접근하여 관리한다. 파일을 객체로 가져오는 java, python 등과 다른 모습이다.

<errno.h>헤더를 이용해 파일 처리에서 발생하는 오류 메시지를 확인할 수 있다.
