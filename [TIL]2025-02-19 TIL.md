# 250219

# JavaScript

현재 웹페이지 표준인 HTML5의 많은 부분을 차지하고 있음

함수형 언어, 객체지향언어, prototype 모두 포함하고 있어 코드 작성이 어려움

## JQuery

JavaScript만으로 구현된 라이브러리

JavaScript를 좀 더 쉽게 사용하기 위해 만들어짐

### selector

```jsx
function myFunction() {
    // 색 바꾸기
    $('h1, div').css("color", "red");
    // 삭제하기
    $('h1, div').remove();
    // text() : 해당 태그의 텍스트를 가져오기
    // text(바꿀 문장) : 해당 태그의 텍스트를 바꾸기
    $('#result').text("바꿀 문장");
    // # : 아이디로 검색하기
    $('#result').css("color", "red");
    // . : 클래스로 검색하기
    $('.mc').css("background-color", "yellow");
    // > 혹은 공백 : 자식 선택자, 후손 선택자
    $('div > ol').css("color", "blue");
    // + 혹ㅇ느 ~ : 동위 선택자
    // +는 바로 뒤에 있는 형제 선택자, ~는 뒤에 있는 모든 형제 선택자
    alert($('#css ~ li').text());
}
```

### 변수 지정

let으로 변수를 지정할 수 있음

location라는 변수에 class가 loc인 값 중 선택된 값을 할당함

input은 일반적인 글자가 아니라 text상자이므로 text()로 입력값을 바꾸는 것이 아니라 val로 해당 text박스의 값을 바꾸어야함.

```html
    <select onchange="myFunction()">
        <option class="loc">서울</option>
        <option class="loc">대구</option>
        <option class="loc">부산</option>
        <option class="loc">대전</option>
    </select>
    <br>
    <input type="text" id="input" value="">
    <button onclick="myFunction()">Try it</button>
```

```jsx
let location = $('.loc:selected').text()
$('#input').val(loc);
```

### for 문

for문을 대신해 each method를 사용한다.

```html
 $("[type=checkbox]:checked + span").each(function (idx,item) {
        console.log(item + '가 선택되었습니다.');}
    );
```

idx는 자료에 순서대로 부여되는 번호

item은 선택된 문서 객체로 jQuery 객체가 아니기 때문에 jQuery로 바꿔서 사용해야함

### 위치 선택

- append()
    - 자식으로 붙여넣고 제일 마지막 자식으로 붙임
- prepend()
    - 자식으로 붙여넣고 첫번째 자식으로 붙임
- after()
    - 형제로 붙여넣고 바로 다음 형제로 붙임
- before()
    - 형제로 붙여넣고 바로 이전 형제로 붙임

### 이미지

```html
<img src="img/car/jpg" width=200>
```

### this

현재 사용하는 문서 객체를 반환하는 함수

jQuery 객체로 변환하기 위해 $()로 감싸야한다