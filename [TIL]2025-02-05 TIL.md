# 250205

# 객체지향

## class

기존의 데이터 타입을 이용해 새로운 데이터 타입을 만드는 것

해당 데이터 타입은 변수(field)와 함수(method)를 가진다.

## instance

class에 정의된 field와 method를 사용하기 위해 heap 메모리에 일정 공간을 확보해야한다. 이렇게 확보된 공간을 instance 혹은 객체라고 부른다.

## constructor

자바의 모든 class는 적어도 하나의 constructor를 가짐

명시적으로 작성하지 않아도 default 생성자가 추가됨

모든 과정이 객체로 이루어지는데 객체를 만들지 못하는 class는 필요가 없기 때문

return 타입이 따로 없으며 class의 이름과 같아야함

객체가 생성될 때 new 키워드와 함께 호출되며 객체가 가지는 필드에 대한 초기화 등의 작업을 함

## JVM 메모리 구조

JVM은 메모리를 용도에 따라 5개 부분으로 나누어서 관리

- Register
    - 현재 실행 중인 명령어의 주소 저장
- Runtime Constant Pool
    - Class Constant pool에서 읽어온 상수 값, 클래스 메타데이터를 저장
    - Class Constant pool은 class에 포함된 상수 값으로 컴파일 시 생성됨
- Method Area
    - method 바이트코드 저장
    - Static으로 호출된 field, method 등 저장
        - static method 내부에 있는 지역 변수는 stack에 저장
- Heap
    - Runtime Constant Pool, Method Area가 Heap에 포함됨
- Call Stack(일반적으로 Stack이라고 함)
    - method가 실행되기 위한 매개변수, 지역변수 등 저장

## Static

static은 instance 없이 사용할 수 있도록 하는 키워드

class에 static으로 선언된 field가 있다면 해당 변수는 class의 모든 instance에서 접근 가능. 즉 인스턴스 간의 연결이 강해져 추천하지 않는 방법이다.

## method overloading

같은 이름의 method가 여러개 존재하는 것을 의미

같은 이름이지만 매개변수가 달라짐에 따라 다른 함수가 호출될 수 있도록 함

## package & import

source code를 효율적으로 관리하고 재사용하는 방법

class의 source code나 class file을 package로 묶어 재사용

## access modifier

public : 어디서나 접근 가능한 field, method, constructor, class 선언에 사용

protected : 기본적으로 default와 같지만 상속관계에 있는 class에서 field, method, constructor를 사용할 수 있도록 선언

default : 같은 package 내의 다른 class에서 접근 가능한 field 등 선언. default는 제어자를 입력하지 않은 경우 되는 것으로 ‘default class’ 식의 선언이 불가능함.

private : 다른 class에서는 접근할 수 없는 field, method, constructor 선언

- getter, setter : private 변수를 다른 class에서 접근 및 변경할 수 있도록 도와주는 public 함수
- 싱글턴 패턴을 위해 생성자에 private을 사용하기도 함

## static block

static은 instance없이 실행할 수 있도록 메모리 공간에 올릴 수 있도록 한다.

static만으로 블럭을 만들 수 있다. main이 실행되기 위해 선행되어야하는 메소드를 모아서 사용한다.

```java
package lecture;

class MyTest {

	int aaa;
	static int bbb = statciCall();
	
	public static int statciCall() {
		System.out.println("static method call()");
		return 100;
	}

	static {
		System.out.println("이것은 Static block의 연습입니다. bbb의 값은 : " + bbb);
	}

	public static void main(String args[]) {
		System.out.println("main() 호출");
	}

}
```

## inheritance

상속은 부모 class가 가진 내용을 자식 class가 물려받는 것이다.

```java
// 예시
class A {
	public void X(){
		System.out.println("X 함수입니다.");
	}
}

class B extens A {
	public void Y() {
		System.out.println("Y 함수입니다.");
	}
}
```

예시를 보면 B 클래스는 A로부터 상속을 받기 때문에 함수X와 함수Y를 모두 가지고 있다.

JAVA는 다중 상속을 지원하지 않음. 다중 상속의 효과를 위해서 상속을 이어가면서 트리구조로 이루어지게 됨

자바의 모든 class는 상속 관계를 가짐

## 상속에서의 constructor 특징

```java
// 예시
class A {
	public A(){
		System.out.println("A class의 Constructor입니다.");
	}
}

class B extens A {
	public B() {
		System.out.println("B class의 Constructor입니다.");
	}
}
public class ConstructorTest{

	public static void main(String[] args) {
		B b = new B();
	}
}
// A class의 Constructor입니다.
// B class의 Constructor입니다.
```

사용자가 명시하지 않아도 자동으로 상위 class의 constructor가 호출됨