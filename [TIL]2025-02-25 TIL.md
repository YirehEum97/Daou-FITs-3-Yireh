# 250225

# 꼬맨틀 구현하기(2)

임베딩 모델을 불러오고 강화학습 후 임베딩 벡터를 사용해 코사인 유사도를 구하는 간단한 테스크였다. 오류를 만나기 전까지..

## 문제점

### 메모리 초과

hugginface에서 jinaai/jina-embeddings-v3를 불러온 후 fine tuning을 시도했다. 메모리가 초과되었다. 이를 해결하기 위해

1. Batch size 감소
Batch size를 4까지 줄여 성능을 약간 포기하면서 메모리를 줄이고자했다. 여전히 메모리가 초과된다.
2. LoRA
LoRA를 이용해 fine tuning에 드는 메모리를 매우 줄였다. 여전히 메모리가 초과된다.
3. Batch size 감소 & LoRA
성능을 약간 포기하며 메모리를 적게 쓰는 방법 두가지를 모두 사용했다. 성능이 떨어질까 걱정이 되었지만 메모리 초과로 결과물이 나오지 않아 기우였음을 알게 되었다.

결국 작은 모델을 찾았다.

### 학습 데이터 부족

koBART 계열 모델로 바꿔 fine tuning을 진행했다. 증권 페이지에서 크롤링한 기업의 개요와 각 기업별 최신 뉴스들을 데이터로 1100개의 문서를 얻어 이를 학습시켰다. 이정도의 데이터로는 기업 간의 거리가 크게 달라지지 않는다는 점을 알게 되었다. 한미반도체와 삼성전자가 유사도가 0.999가 나오는 것을 보고 해당 모델을 파기했다.

### 새로 학습 시키기

얻은 데이터로 TF-IDF를 만들었다. 다만 해당 값을 신뢰할 수 있는지 검증할 시간도 없고 성능이나 학습 속도에서 아쉬움이 남아 다른 방법을 찾았다.

### fasttext 모델

fasttext 모델은 기본적으로 n-gram을 이용하기 때문에 단어가 아닌 토큰이 유사도 순위에 들어간다는 단점이 있었다. 이는 입력 데이터를 지정된 토큰으로 한정함으로 해결했다.

## DB 구축

임베딩 벡터가 들어있는 EMBEDDING_TABLE, 당일 정답 단어와의 유사도 순위를 담은 KOMENTLE_WORD_RANK_TABLE을 만들었다. 임베딩 벡터는 300차원으로 했다.