# 250123

# 인덱스

데이터베이스 테이블의 검색 성능 향상을 위해 사용하는 특별한 데이터 구조

도서관의 책 목차와 같은 역할

특정 데이터를 빠르게 찾아갈 수 있는 경로를 제공

### 사용 목적

검색 성능 향상

쿼리 실행 시간 단축

테이블의 특정 컬럼을 기준으로 데이터 정렬 및 관리

## 인덱스 구조

- B-tree 인덱스(가장 일반적인 구조)
- 균형 이진 트리 형태로 데이터 검색에 최적화
- 데이터가 정렬된 상태로 유지되면 검색, 삽입, 삭제 시 효율적
- 루트 노드 → 중간 노드 → 리프 노드 순서로 탐색
- 리프 노드에는 실제 데이터 위치 정보 저장

### B-Tree 인덱스

Balanced Tree 구조로 데이터를 정렬된 상태를 유지하며 검색 및 삽입, 삭제에 걸리는 시간을 단축시킴

### 장점

- 검색 속도 향상 : 데이터베이스에서 SELECT 쿼리를 실행할 때 효율적
- 정렬된 데이터 관리 : 인덱스를 사용하면 데이터가 자동으로 정렬
- 대용량 데이터 처리 : 테이블 크기가 커질수록 인덱스의 효율성이 증가

### 단점

- 추가 저장 공간 필요 : 인덱스는 테이블 외에도 별도의 공간을 차지
- 쓰기 작업 성능 저하 : INSERT, UPDATE, DELETE 작업 시 인덱스도 함께 갱신되어야 함
- 인덱스 과다 생성 문제 : 너무 많은 인덱스는 관리 비용 증가와 성능 저하를 초래

### 인덱스 생성

```sql
-- 단일 컬럼 인덱스
CREATE INDEX idx_customer_name ON customers(name);

-- 결합 인덱스
CREATE INDEX idx_order_customer_date ON orders(customer_id, order_date);

-- 유니크 인덱스
CREATE INDEX idx_email ON users(email);
```

### 인덱스 확인 및 삭제

```sql
SELECT index_name, table_name, uniqueness
FROM user_indexes
WHERE table_name = 'CUSTOMERS';

DROP INDEX idx_customer_name;
```

### 인덱스 관리 팁

자주 사용되는 컬럼에 생성 : WHERE 조건, JOIN, ORDER BY, GROUP BY에 자주 등장하는 컬럼

적절한 인덱스 개수 유지 : 과도한 인덱스는 성능 저하를 초래

쓰기와 읽기 비율 고려 : 읽기 작업이 많은 테이블에 적합

데이터 중복도 파악 : 중복도가 낮은 컬럼은 B-Tree, 높은 컬럼은 Bitmap 인덱스를 추천

### 유의사항

- 인덱스 컬럼의 순서
    - 결합 인덱스에서 SELECT 조건에 자주 사용되는 컬럼을 우선적으로 배치
    - 예: WHERE NAME = ‘John’ AND AGE > 30
    →(NAME, AGE) 순서대로 인덱스 생성
- 쓰기 작업 최적화
    - 데이터 변경 작업이 빈번한 테이블은 최소개의 인덱스 유지
- 카디널리티
    - 카디널리티(값의 고유도)가 높은 컬럼에 인덱스를 생성
    - 예: 주민등록번호 > 성별
- 데이터 크기와 접근 빈도
    - 대용량 데이터 테이블에서 인덱스는 필수

### 인덱스를 타지 못한다는 것의 의미

데이터 베이스에서 인덱스를 타지 못한다는 것은 활용할 수 있는 인덱스가 있음에도 불구하고 SQL 쿼리가 인덱스를 활용하지 않고 테이블 전체를 스캔하여 데이터를 조회하는 경우를 의미

→ 불필요한 데이터 검색으로 인해 성능 저하 발생

### 인덱스를 활용하지 못하게 하는 주요 원인

- 인덱스 컬럼에 함수 또는 연산 사용
- 데이터 타입 불일치
- 와일드카드(%) 사용 위치(와일드 카드가 앞에 있다면 Full Scan을 해야함)
- NULL 값 처리
    - 인덱스 컬럼에서 NULL값은 기본적으로 인덱스에 저장되지 않음
    - NULL 값을 적절한 Default 값으로 처리가 필요함
- OR 조건이 포함된 경우
    - WHERE 절에서 OR 조건이 포함된 경우 일부 조건에만 인덱스를 사용할 수 있음
    - UNION ALL로(혹은 UNION) 조건을 분리하는 것이 나음

## Index Range Scan

튜닝 전략

- 인덱스 스캔 범위 줄이기
- 테이블 액세스 빈도 줄이기
- 인덱스를 구성하는 선행 컬럼을 조건절에 사용
- Equal 조건으로 자주 쓰이는 컬럼을 선행 컬럼으로 설정

예시

```sql
-- 사례 1
Index = id + name
SELECT *
	FROM CUSTOMERS
WHERE name = '손흥민';

-- 사례 2
Index = name + id
SELECT *
	FROM CUSTOMERS
WHERE name = '손흥민';
```

사례 1은 손흥민을 찾기 위해 테이블을 FULL SCAN 해야함. 사례 2처럼 정렬하면 일부만 검색해 데이터를 찾을 수 있음

```sql
-- 사례 1
Index = birthyear + name + mobile
SELECT *
	FROM CUSTOMERS
WHERE birthyear between 1990 AND 2000
	AND name = '손흥민'
	AND mobile = '01012345678';
	
-- 사례 2
Index = name + birthyear + mobile
SELECT *
	FROM CUSTOMERS
WHERE birthyear between 1990 AND 2000
	AND name = '손흥민'
	AND mobile = '01012345678';
	
-- 사례 3
Index = mobile + birthyear + name
SELECT *
	FROM CUSTOMERS
WHERE birthyear between 1990 AND 2000
	AND name = '손흥민'
	AND mobile = '01012345678';
```

사례 1은 birthyear 기준으로 데이터를 여러개 뽑은 후 그 안에서 탐색

사례2는 동명이인을 추출 후 그 안에서 탐색

사례3은 일반적으로 유일값을 바로 찾을 수 있음

### Reverse Key Index

키의 앞부분이 특정 영역에 몰릴 수 있다. 예를 들어 날짜를 키로 지정해두는 경우 특정 월의 데이터에 여러명이 작업을 한다면 ping이 발생하게 된다. Reverse Key Index는 키를 byte단위로 거꾸로 키를 저장해 데이터가 여러 블록에 나눠져서 저장되기 때문에 이러한 문제를 해결할 수 있다. 다만 키가 순서대로 저장된 것이 아니기 때문에 순차 탐색이 불가능하고 Full Scan이나 Equal 탐색만 지원한다. 애초에 글자기준이 아니라 byte 기준으로 거꾸로 저장한거라서 대소 비교도 어려울 수 있다.