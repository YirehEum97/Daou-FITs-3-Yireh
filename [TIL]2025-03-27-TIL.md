# 자동으로 거래 처리하는 sql

날짜가 바뀌면 자동으로 trades에 있는 거래들을 처리해서 수정하고 거래된 내역을 transactions에 쌓는 업무는 쿼리로도 해결이 가능할 것으로 봤다. 따라서 해당 쿼리를 만들고 sql 파일을 불러온 다음에 OCI로 쿼리를 수행하는 것을 구현했다.

```sql
DECLARE
  v_today DATE := :today;
  CURSOR cur_trades IS
    SELECT t.trade_id, t.left_amount, t.order_price
    FROM Trades t
    JOIN Stocks_Price s ON t.ticker = s.ticker
    WHERE t.trade_success IN (0, 1)
      AND s.stock_date = v_today
      AND t.order_price BETWEEN s.low AND s.high;
  
  -- FOR 루프의 인덱스 변수 대신 로컬 변수 선언
  l_trade_id   Trades.trade_id%TYPE;
  l_left_amount Trades.left_amount%TYPE;
  l_order_price Trades.order_price%TYPE;
BEGIN
  FOR rec IN cur_trades LOOP
    -- 인덱스 변수(rec)는 읽기 전용이므로, 로컬 변수에 복사하여 사용
    l_trade_id := rec.trade_id;
    l_left_amount := rec.left_amount;
    l_order_price := rec.order_price;
    
    INSERT INTO Transactions (
      transaction_amount, 
      transaction_price, 
      created_at, 
      updated_at, 
      trade_id
    )
    VALUES (
      l_left_amount,
      l_order_price,
      SYSDATE,
      SYSDATE,
      l_trade_id
    );
    
    UPDATE Trades
    SET left_amount = 0,
        trade_success = 2,
        updated_at = SYSDATE
    WHERE trade_id = l_trade_id;
  END LOOP;
  
  COMMIT;
END;

```

해당 쿼리는 다음의 함수를 통해 불러와진다.

```c
sword executeProcessTransactionWithDate(const char* dateStr) {
    // 날짜 문자열 길이 검증 ("YYYYMMDD" 형식이어야 함)
    if (strlen(dateStr) != 8) {
        printf("invalid date: %s\n", dateStr);
        return OCI_ERROR;
    }

    // "YYYYMMDD" 형식에서 연, 월, 일을 추출하여 정수로 변환
    char yearStr[5] = { 0 }, monthStr[3] = { 0 }, dayStr[3] = { 0 };
    strncpy(yearStr, dateStr, 4);
    strncpy(monthStr, dateStr + 4, 2);
    strncpy(dayStr, dateStr + 6, 2);
    int iYear = atoi(yearStr);
    int iMonth = atoi(monthStr);
    int iDay = atoi(dayStr);

    // SQL 스크립트 파일 읽기 (파일 내에는 PL/SQL 블록 또는 SQL 문이 있고, :today 바인드 변수를 포함해야 함)
    FILE* fp = fopen("./query/process-transaction.sql", "r");
    if (!fp) {
        printf("FAIL: open SQL file(./query/process-transaction.sql)\n");
        return OCI_ERROR;
    }
    fseek(fp, 0, SEEK_END);
    long fileSize = ftell(fp);
    rewind(fp);

    char* fileContent = (char*)malloc(fileSize + 1);
    if (!fileContent) {
        fclose(fp);
        printf("메모리 할당 실패.\n");
        return OCI_ERROR;
    }
    size_t bytesRead = fread(fileContent, 1, fileSize, fp);
    fileContent[bytesRead] = '\0';
    fclose(fp);

    // Statement 핸들 할당 및 SQL 문 준비
    OCIStmt* stmtp = NULL;
    sword status = OCIHandleAlloc(envhp, (dvoid**)&stmtp, OCI_HTYPE_STMT, 0, NULL);
    if (status != OCI_SUCCESS) {
        free(fileContent);
        printf("OCIHandleAlloc failed\n");
        return status;
    }

    status = OCIStmtPrepare(stmtp, errhp, (text*)fileContent, (ub4)strlen(fileContent),
        OCI_NTV_SYNTAX, OCI_DEFAULT);
    free(fileContent);
    if (status != OCI_SUCCESS) {
        OCIHandleFree(stmtp, OCI_HTYPE_STMT);
        printf("OCIStmtPrepare failed\n");
        return status;
    }

    // 날짜를 OCIDate 구조체로 변환 (시간은 00:00:00으로 설정)
    OCIDate oci_date;
    OCIDateSetDate(&oci_date, iYear, iMonth, iDay);
    OCIDateSetTime(&oci_date, 0, 0, 0);

    // SQL 스크립트 내의 :today 바인드 변수에 OCIDate 값을 바인딩
    OCIBind* bindhp = NULL;
    status = OCIBindByName(stmtp, &bindhp, errhp, (text*)":today", -1,
        (dvoid*)&oci_date, sizeof(oci_date),
        SQLT_ODT,  // Oracle Date 타입
        NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
    if (status != OCI_SUCCESS) {
        OCIHandleFree(stmtp, OCI_HTYPE_STMT);
        printf("OCIBindByName failed\n");
        return status;
    }

    // SQL 문 실행 (쿼리가 PL/SQL 블록이면 한 번 실행, SQL 문이면 적절한 반복 횟수를 지정)
    status = OCIStmtExecute(svchp, stmtp, errhp, 1, 0, NULL, NULL, OCI_DEFAULT);
    if (status == OCI_SUCCESS || status == OCI_SUCCESS_WITH_INFO) {
        printf("process-transaction.sql executed successfully\n");
    }
    else {
        printf("process-transaction.sql execution failed\n");
    }

    // 커밋 및 자원 해제
    OCITransCommit(svchp, errhp, OCI_DEFAULT);
    OCIHandleFree(stmtp, OCI_HTYPE_STMT);

    return status;
}
```

이제 executeProcessTransactionWithDate(”19950626”)와 같이 입력하면 자동으로 처리가 된다.
