# 250110

# 컴퓨터 통신과 네트워크

컴퓨터 통신 : 컴퓨터나 기기가 데이터를 주고 받는 통신

네트워크 : 컴퓨터 여러 대가 데이터를 주고 받는 통신 시스템

인터넷 : 네트워크를 하나로 묶는 기술. 전 세계의 수많은 컴퓨터를 연결한 네트워크 집합

### 네트워크 종류

근거리 네트워크 : 수미터~ 1킬로 이내의 컴퓨터를 연결

장거리 네트워크 : 국가 전체를 연결하거나 국가 간의 연결

개인 네트워크 : 가까운 주변 장치들을 연결

초근거리 네트워크(NFC) : 수 센티미터 거리에서 통신

## LAN 토폴로지

### 스타형

중앙에 위치한 한 장치에 모든 장치를 연결
중앙 장치를 통해 데이터 전송이 이루어짐

### 링형

장치들을 원형으로 연결

데이터 전송을 위해 token 사용

### 버스형

하나의 통신 회선에 장치들을 연결

데이터 목적지 주소와 함께 버스에 실어 보냄

## 통신 분류

### 방향에 따른 분류

단방향 통신 : 한쪽으로만 통신 가능. 모스 부호, 방송 등이 있다.

양방향 통신 : 양쪽으로 동시에 통신 가능. 컴퓨터, 휴대폰 등이 있다.

반양방향 통신 : 양방향 통신이지만 어느 순간에는 단방향. 무전기

### 통신 대상에 따른 통신의 분류

유니캐스트 : 일대일 연결. 전화

브로드캐스트 : 보내는 기기 한대와 불특정 다수 연결. 방송

멀티캐스트 : 보내는 기기 한대와 받는 기기 여러대를 연결. 집단 화상회의

## 네트워크 통신의 계층 모델

물리적 연결 계층 + 프로토콜 계층을 합쳐 각 계층으로 정의한다.

### 계층의 독립성

각 계층은 자신보다 상위 계층의 작업을 알지 못한다.

각 계층은 같은 계층끼리만 통신한다.

### 네트워크 통신의 다섯 계층

물리 계층 : LAN에서 통신기기의 연결을 담당

데이터 링크 계층 : 인접한 기기 간에 오류 없이 데이터를 전송하도록 제어

네트워크 계층 : 여러 LAN을 하나로 묶어서 인터넷을 만드는 핵심 계층

전송 계층 : 네트워크 계층에서 목적지까지 오면서 생기는 오류를 보정

응용 계층 : 인터넷을 사용하는 각종 응용 프로그램이 위치하는 계정

### OSI 7layers

다섯 계층 구조에서 응용 계층이 세션 계층, 표현 계층, 응용 계층으로 세분화 됨

### TCP/IP 모델

현재 가장 많이 사용하는 네트워크 계층 모델

로컬 네트워크 : 물리 계층, 데이터 링크 계층에 해당한다

IP : 네트워크 계층에 해당한다.

TCP : 전송 계층에 해당한다.

HTTP, FTP : 응용 계층에 해당한다.

### 헤더

데이터 전송을 위해 추가되는 자료구조로 각 계층에서 전송을 위해 추가된다.

IP 헤더 : 버전, 헤더 길이, 서비스 타입, 전체 길이, ID, 조각 상태, 조각 위치, 생존 기간, 프로토콜 등 정보 포함

MAC 주소 : 송신측 MAC 주소, 수신측 MAC 주소, 이더넷 타입 포함

TCP 헤더 : 송수신 측 포트 번호, 순서 번호 등이 포함. 20바이트

UDP 헤더 : 송수신 측 포트 번호, 데이터 길이, 체크섬으로 이루어져있음. 8바이트

### 프로토콜

네트워크 통신에서 데이터를 주고 받을 때의 규칙과 절차를 정의한 것

컴퓨터나 네트워크 장치들이 서로 정보를 교환할 때, 프로토콜을 통해 서로의 요구 사항과 동작을 이해하고 일관되게 처리

### 물리 계층

실제 전송 매체에서 데이터를 전기적 신호나 광신호로 전송하는 프로토콜

전송 매체 : 신호를 한쪽에서 다른 쪽으로 전달하는데 사용되는 물질

### 데이터 링크 계층

같은 네트워크 내에서 패킷을 물리적 주소로 전송

프레임 : 데이터 링크 헤더 + 패킷

전송 방식

- 이더넷 : 유선 LAN에서 널리 사용되는 프로토콜. MAC 주소를 사용해 네트워크 장치 간에 데이터를 전송
- Wi-Fi : 무선 네트워크에서 사용되는 프로토콜. 와이파이를 통해 장치 간 데이터 전송

MAC 주소 : 데이터 링크 계층에서 사용되는 물리적 주소

네트워크 인터페이스 카드나 네트워크 장치에 고유하게 할당

### 데이터 링크 계층에서의 오류 처리

오류 처리 방법

- 데이터 중복 전송 : 송신 측에서 각 데이터를 두번씩 보내는 방법
- 중복 정보 전송 : 전송할 데이터에 대한 중복 정보(오류를 찾는 오류 탐색코드와 오류를 찾고 보정하는 오류 보정 코드가 있다.)를 데이터와 함께 보내는 방식
    - 수직 중복 검사 : 데이터에 대한 패리티 비트라는 중복 정보를 구해 데이터와 함께 수신측으로 전송
        - 패리티 비트 : 보내려는 데이터에 1비트를 추가하여 전체 1의 개수가 짝수 혹은 홀수가 되도록 만드는 방식
    - 세로 중복 검사 : 데이터를 행렬의 형태로 구성해 오류 검출
    - 체크섬 : 데이터를 일정 크기로 자르고 더한 값이 같은지 확인
    - CRC : 주어진 다항식에 대해서 XOR 연산을 해가며 마지막 남은 나머지를 비교하여 오류 확인

### 네트워크 계층

데이터를 목적지까지 라우팅하고 전달하는 역할

라우팅 : 어떤 길로 패킷을 보낼지 결정

라우터 : 패킷을 어느 쪽 네트워크로 보낼지 결정하는 통신 장비

라우팅 테이블 : 데이터를 어디로 보낼 지 나타낸 표

### IP 주소

인터넷에 연결된 여러 장치를 식별하는 번호. IP주소는 네트워크를 통해 장치를 찾고 MAC 주소는 로컬 네트워크에서 해당 장치와 직접 통신

서브넷 마스크 : IP 네트워크에서 서브넷을 나누기 위해 사용. 네트워크 주소는 서브넷 마스크에서 1로 설정, 호스트주소는 0으로 설정한다. 처음 24비트가 네트워크 주소, 나머지 8비트가 호스트 주소인 경우 11111111.11111111.11111111.00000000으로 255.255.255.0이다.

IP주소 : 192.168.1.10

서브넷 마스크 : 255.255.0.0

네트워크 주소 : 192.168.0.0

호스트 주소 : 192.168.1.10

기존의 IP주소는 주소가 부족해 128비트 체계인 IPv6가 생김

DHCP : 네트워크에서 장치들이 IP 주소 및 기타 필요한 네트워크 설정을 자동으로 할당 받을 수 있도록 도와주는 시스템

DNS : 도메인 네임과 IP 주소를 테이블 형태로 모아놓은 서버

### ping

특정 호스트가 네트워크에 연결되어 있는지 확인할 수 있는 도구

### 전송 계층

데이터 전송의 정확성과 순서를 보장하는 계층

네트워크 계층에서 올라온 데이터를 정리하고 데이터를 프로그램으로 전달

### TCP

송신측과 수신측의 연결이 이루어진 것을 확인한 다음에 데이터를 전송하는 프로토콜 신뢰성이 요구되는 데이터 전송 시 사용

### UDP

송신측과 수신측의 연결 과정을 거치지 않고 일방적으로 데이터를 보내고 수신측으로부터 응답도 따로 받지 않는 프로토콜

### 포트

응용 프로그램 구분을 위해 전송 계층이 사용하는 주소

well-known ports : 자주 사용하거나 중요한 프로그램의 포트 번호를 전 세계적으로 합의하여 고정

### 응용 계층

사용자가 직접 상호작용하는 네트워크의 최상위 계층. 사용자 요청을 처리

### 월드 와이드 맵(www)

인터넷에서 정보를 쉽게 찾을 수 있도록 제공하는 서비스

### 보안

SSL : 웹 클라이언트와 서버 사이의 보안 연결. 데이터를 암호화하여 보내고 받은 데이터를 복호화한다.

TLS : SSL의 후속 프로토콜로 SSL의 취약점을 개선하고 더 강력한 암호화를 제공

원격지의 컴퓨터를 인터넷을 통해 접속하여 자신의 컴퓨터처럼 사용할 수 있는 원격 접속 서비스

### FTP

인터넷을 통해 컴퓨터 간 파일 송수신을 위한 프로토콜

### SSH

네트워크 상에서 컴퓨터나 서버와 안전하게 통신하고 원격으로 관리할 수 있는 프로토콜

### 전자우편

SMTP, POP3, IMAP 등의 프로토콜을 통해 인터넷으로 편지를 교환하는 서비스

### 방화벽

네트워크 보안 강화를 위해 네트워크 트래픽을 제어하는 장치 또는 시스템

## 기타

### 서비스 배포

Amazon EC2, Heroku, Ngrok, Github 등이 있음

### 터널링

VPN은 터널링 기술을 사용해 데이터를 암호화된 ‘터널’을 통해 전송.