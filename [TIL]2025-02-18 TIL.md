# 250218

# Session

Server는 기본적으로 client를 구분할 수 없음

### Cookie

client의 브라우저에 정보를 저장해 server는 client를 파악할 수 있음

client는 브라우저의 정보를 열람, 수정 및 삭제가 가능해 보안성이 매우 떨어짐

### Session

HttpSesssion class의 instance로 WAS안에 저장됨

한 client에게 하나의 instance만 할당됨

해당 instance는 cookie처럼 client를 구분하기 위해 사용됨

정보가 WAS에 저장되어 관리되기 때문에 보안에 좋음

# web 발전 과정

## Static Web

client가 서버로부터 resource를 받아 실행하는 web의 형태

구현의 제약이 많음

## Dynamic Web

servelt으로 동적으로 content를 생성해서 web brower에 보냄

코드 내부에 html을 out.println으로 입력하는 불편함이 있음

## JSP

jsp를 사용해 html 형식의 자바 파일을 호출해 html코드를 자바코드로 변환해 사용

입출력, 로직 연산, DB연결 등 모든 것을 jsp에서 처리하다보니 유지보수가 어려움

## MVC pattern

servlet을 controller, model 역할로 사용하고 JSP를 view 역할로 사용

# JSP

### sriptlet

일반 자바 코드를 넣을 수 있는 블럭

해당 블럭을 이용해 html 내부에서 자바코드를 실행할 수 있음

```html
<% 코드 입력 %>  <!-- 함수 실행 -->
<%= String %>  <!-- String 출력 -->
```