# 250226

# 꼬맨틀 구현하기(3)

꼬맨틀 시스템 구조이다.

## Query

xml 파일로 된 sql 쿼리가 들어있는 파일들이다.

### **Q1**

사용자가 입력한 정답이 몇 위에 있는 단어이며 얼마나 정답과 유사한지 결과를 반환한다.

(select) selectCosineSimilarity

in : Str(try)

out : V1

DB2

### **Q2**

사용자가 입력한 단어가 사전 단어에 있는지 비교하여 결과를 반환한다.

(select) isWord

in : Str(try)

out : boolean

DB1

### Q3

오늘의 정답을 입력 받으면 자동으로 정답 단어와 사전 단어의 코사인 유사도를 계산해 DB에 입력한다. 함수는 미리 정의된 ‘cosine_similarity’ 함수를 사용한다. 정답 단어 또한 사전 단어에 있기 때문에 해당 단어의 순위는 0이 되며 이후 함수에서 순위가 0이면 정답으로 인정된다.

(insert) insertTodayWord

in : Str(ans)

DB1, DB2

### Q4

사용자가 입력한 결과들을 DB에 저장한다.

(insert) insertTrials

in : V2

DB3

## VO

꼬맨틀에서 사용하는 VO는 하나의 파일로 관리한다.

### field

String.word

int.rank

double.cosineSimilarity

String.uuid

**constructor**

### V1 (rank, cosineSimilarity)

### V2 (uuid, word, cosineSimilarity, rank)

## DAO

### DAO1

사용자가 정답을 입력하면 정답 테이블에서 유사도와 순위를 반환한다.

V1 tryAnswer(String try)

selectOne(Q1)

### DAO2

사용자가 입력한 문자가 사전 단어에 있는지 여부를 판단한다.

boolean isWordInList(String try)

selectOne(Q2)

### DAO3

매일 새로운 정답에 대한 유사도를 계산해 정답 테이블에 넣어둔다.

void insertTodayWord(String ans)

insert(Q3)

### DAO4

사용자 입력을 로그 파일에 쌓는다.

void insertTrials(V2)

insert(Q4)

## DB

### DB1

EMBEDDING_TABLE

사전 단어와 사전 단어의 임베딩 벡터를 저장한 DB. 임베딩 벡터가 300차원이라 컬럼이 300개가 있다.

- (PK)word, varchar2
- ‘0’, number
- ‘1’, number
- ‘299’, number

### DB2

KOMANTLE_WORD_RANK_TABLE

매일 정답 단어에 대해 유사도를 미리 계산하고 순위와 유사도를 담은 테이블

- (PK,FK) word, varchar2
- rank, number
- cosine_similarity, number

### DB3

KOMANTLE_TRIALS

사용자 입력을 기록한 로그데이터

- (PK) ID   ← 고유한 로그 id
- cosine_similarity, number
- try_seq, number
- (FK) uuid
- (FK) word