# 250104

## 컴퓨터 구조

### 컴퓨터 구성

하드웨어 : 컴퓨터를 구성하는 기계 장치. 소프트웨어가 지시한 일을 수행

소프트웨어 : 하드웨어의 동작을 지시하고 제어하는 역할을 하는 프로그램. 프로그램에 필요한 데이터

### 하드웨어의 구성

**중앙처리장치**(CPU) : 모든 계산과 명령 처리를 담당

주기억장치(RAM) : 프로그램 실행에 필요한 데이터를 일시적으로 저장

보조기억장치(SSD, HDD 등) : 프로그램과 데이터를 장기적으로 저장

입출력장치(모니터, 마우스 등) : 데이터를 입력하거나 출력

### 중앙처리장치

제어장치 : 중앙처리장치에서 일어나는 모든 작업을 통제하고 관리

연산장치 : 제어장치의 신호에 따라 산술 연산과 논리 연산 수행

레지스터 집합 : 중앙처리장치에서 명령어를 수행하는 동안 필요한 정보 저장

### CPU

성능 결정 요소 : 버스 대역폭, 레지스터, 메모리, 클럭 속도, 코어 수, 캐시메모리, 명령어 집합, 프로세서 아키텍처

### GPU

그래픽 처리에 특화된 전자 장치

단순한 형태의 코어를 대량으로 집적하여 단순 연산을 병렬로 수행

### NPU

인공지능 반도체로 인공신경망 알고리즘을 효율적으로 계산하는데 특화된 반도체

행렬연산, 벡터연산, 텐서연산에 최적화된 아키텍처 제공

### 폰 노이만 구조

컴퓨터 아키텍처의 기본적인 모델로 현대 컴퓨터는 대부분 폰노이만 구조를 따름

버스라는 구조를 사용해 명령어, 데이터 등이 이동한다.

버스는 장치간 데이터를 전송하는 통로로 주소를 전달하는 **주소버스**, 데이터를 이동하는 **데이터버스**, CPU와 장치간의 제어를 전달하는 **제어버스**로 구성되어 있다.

### 프로세서

멀티프로세서 : 두개 이상의 포로세서를 하나의 컴퓨터 시스템에서 동시에 사용하여 작업을 처리하는 아키텍처

멀티코어 시스템 : 하나의 칩에 여러개의 프로세서 코어가 내장된 컴퓨터 시스템. 

Thread : 프로세스의 실행 단위

멀티쓰레딩 : 하나의 프로세스 내에서 여러 쓰레드를 실행하는 방식

하이퍼쓰레딩 : 하나의 물리적인 CPU코어를 두개의 논리적인 코어처럼 동작하게 만들어 멀티태스킹 성능을 향상시키는 기술

## 운영체제

컴퓨터 전체를 관리하고 운영하는 소프트웨어아이콘 추가

데스크탑, 서버용, 스마트폰, 임베디드 등 다양한 시스템을 위한 운영체제가 존재함

### 용도별 특징

데스크탑 : 하드웨어와 소프트웨어간의 상호작용 관리

스마트폰 : 모바일 장치에서 실행됨. 데스크탑 OS와 역할 유사

서버 : 대규모 트래픽 처리할 수 있고 보안 기능, 안정성, 네트워크 성능에 중점

임베디드 : 임베디드 시스템에서 사용가능. 크기가 작고 기능이 적음

### BIOS

basic input/output system

컴퓨터 시스템에서 하드웨어와 운영체제 사이의 기본적인 상호작용을 담당하는 소프트웨어

컴퓨터 전원이 켜지면 바이오스가 먼저 하드웨어를 점검

부트스트랩 코드를 메인 메모리에 올려서 실행

### 가상운영체제

물리적인 하드웨어 자원 위에 가상의 운영체제를 실행할 수 있도록 하는 시스템

### Docker

컨테이너 기반의 가상화 플랫폼

컨테이너 : 어플리케이션과 그에 필요한 시스템툴, 환경설정, dependencies 등을 하나로 묶은 것

### 커널

운영체제의 핵심적인 기능을 모아 놓은 것

주요기능 : 프로세스 관리, 메모리 관리, 파일 시스템, 입출력 관리, 프로그램 간 통신 환경 제공

### 커널_프로세스 관리

프로그램으로 작성된 작업 절차를 실제로 실행에 옮김

프로세스 상태 : new, ready, running, waiting, blocked, terminated(exit)

프로세스 스케줄링 알고리즘 : FCFS(FIFO, 먼저 들어온 프로세스 우선 실행), SJF(실행시간이 짧은 프로세스 먼저 실행)

### 커널_메모리 관리

시분할 시스템의 메모리 관리자 : 프로세스의 독립적인 작업 영역을 보장해줌

스왑 : 프로그램의 모듈 중 사용중이지 않은 모듈을 스왑 영역에 저장해둔 후 이후 사용할 때 메모리에 올라와 있는 모둘과 교체

### 커널_페이징

메모리 공간을 고정 크기의 블록인 프레임으로 나누고 프로세스는 페이지라는 고정 크기의 블록으로 분할되어 메모리에 분산하여 할당

페이지 교체기법 : LRU(가장 오래 사용되지 않은 페이지를 교체), FIFO(먼저 들어온 페이지를 교체), LFU(가장 적게 참조된 페이지를 교체)

### 커널_파일 시스템

사용자에게 접근 권한이 있는 파일만 제공하고 특정 위치에 파일을 보관하는 것을 방지

블록 : 저장 장치에서 파일을 묶어서 관리하는 일정 크기

파일 이름 규칙

1. 파일 이름.확장자 형태
2. 파일 이름에는 마침표 여러번 사용 가능 : 마지막 마침표 다음이 확장자
3. 경로 포함해서 최대 255자

디렉터리 : 서로 관련 있는 파일을 모아 놓은 것

## 수학

### 진법

사용할 수 있는 숫자의 개수와 각 숫자의 위치값을 정의한 수 체계

일반적으로 우리가 사용하는 수 체계는 10진법이다.

컴퓨터에서 주로 사용하는 수 체계는 2진법, 8진법, 16진법이 있다.

153(n) : n진법으로 표시된 153이라는 숫자이다. 10진법으로 바꾸면 아래와 같다.

153(n) = 1*(n)^2 + 5*n + 3