# 250106

# 이산수학

정의 : 불연속적인 객체를 연구하는 수학

## 집합

정의 : 순서가 지정되지 않은 원소모임

집합 안에 있는 개체를 원소라고 부름. a가 집합의 원소일 경우 a∈A로 표기함.

### 집합의 종류

전체집합 : 모든 원소를 포함하는 집합. U로 표기

공집합 : 원소가 없는 집합.

부분집합 : 집합 A의 모든 원소가 집합 B에 포함될 경우 A는 B의 부분집합이라고 한다.

멱집합 : 집합 A의 모든 부분집합의 집합은 A의 멱집합이라고 부른다.

합집합 : 두 집합에 하나라도 속하는 원소들로 이루어진 집합

교집합 : 두 집합에 모두 속하는 원소로 이루어진 집합

여집합 : 전체 집합의 원소 중 A에 속하지 않은 모든 원소로 이루어진 집합

차집합 : 집합 A에 속하면서 집합 B에 속하지 않는 원소로 이루어진 집합을 A - B로 표시한다.

## 순열과 조합

### 순열

정의 : 서로 다른 원소들의 순서를 고려하여 일렬로 배열하는 것.

n개의 원소 중 r개 원소를 나열하는 경우의 수는 아래와 같이 계산한다.

**P(n,r) = nPr = n! / (n-r)!**

### 조합

정의 : 서로 다른 원소를 순서 고려하지 않고 배열하는 것.

n개의 원소 중 r개 원소를 나열하는 경우의 수는 아래와 같이 계산한다.

**C(n,r) = nCr = n! / (n-r)!r!**

### 관계(relation)

정의 : 집합의 데카르트 곱의 부분집합

a∈A, b∈B 일때 a에서의 b로의 관계를 정의할 수 있다. 이때 이 관계를 (a,b)로 표시하고 a는 first coordinate, b는 second coordinate라고 한다.

### 관계의 종류

이진관계 : 두개의 집합 사이에서 정의된 관계

반사적 관계(reflexive) : 모든 원소가 자기 자신과 관계를 가지는 집합

대칭적 관계(symmetric) : (a,b)가 원소이면 (b,a)를 항상 원소로 가지는 집합

추이적 관계(transitive) : (a,b), (b,c)를 원소로 가지면 (a,c)를 항상 원소로 가지는 집합

### 함수

정의 : first coordinate가 중복되지 않는 관계의 부분집합

정의역(Domain) : first coordinate의 집합

공역(Codomain) : second coordinate가 될 수 있는 원소의 집합

치역(range) : second coordinate의 집합

### 함수의 종류

단사(injection) : second coordinate가 중복되지 않는 함수

전사(surjection) : Codomain과 Range가 같은 함수

전단사(bijection) : 단사이면서 전사인 함수

### 명제

참이나 거짓으로 명확히 판별할 수 있는 문장.

명제 합성 : 논리적 연산자와 다른 명제들로 구성

부정 : T를 F로 F를 T로 바꾸는 연산

논리곱 : 모두 참일때만 참을 반환하는 연산

논리합 : 하나라도 참일때 참을 반환하는 연산

배타적 논리합 : 하나만 참일때 참을 반환하는 연산

함축 : a라면 b이다 구조로 이루어진 연산. a가 참이고 b가 거짓일 때를 제외하면 참을 반환한다.

상호조건 : a와 b가 동일함을 보이는 연산. a와 b가 동일한 결과일 때 참을 반환한다.

**논리연산자 우선순위** : 부정 > 논리곱 > 배타적 논리합 > 논리합 > 함축 > 상호조건

항진명제 : 항상 참인 명제

모순 명제 : 항상 거짓인 명제

### 명제 함축(implication)

p → q에 대해서

역 : q → p

이 : ~p → ~q

대우 : ~q → ~p

명제와 대우는 동치이다.

### 명제 법칙

드모르간 법칙

- ~(p∧q) ≡ ~p∨~q
- ~(p∨q) ≡ ~p∧~q

교환법칙

- p∨q ≡ q∨p
- p∧q ≡q∧p

결합법칙

- (p∧q)∧r ≡ p∧(q∧r)
- (p∨q)∨r ≡ p∨(q∨r)

분배법칙

- (p∨(q∧r)) ≡ (p∧q)∨(p∧r)
- (p∧(q∨r)) ≡ (p∨q)∧(p∨r)

흡수법칙

- p∧(p∨q) ≡ p
- p∨(p∧q) ≡ p

### Boolean

원소로 0과 1을 가지고 합, 차, 곱의 연산을 가지는 algebra

**Theorems**

- 이중보수법칙
- 멱등법칙
- 항등법칙
- 지배법칙
- 교환법칙
- 결합법칙
- 분배법칙
- 드모르간 법칙
- 흡수법칙

### Minterm

입력 변수들의 모든 가능한 곱의 조합

A,B 두가지 변수가 있을 때 아래와 같이 표기한다.

- 0,0 = A’B’ = m0
- 0,1 = A’B = m1
- 1,0 = AB’ = m2
- 1,1 = AB = m3

계산은 아래와 같이 한다.

- m1 + m3 = A’B + AB = (A’+A)B = B

### Maxterm

입력 변수들의 모든 가능한 합의 조합

A,B 두가지 변수가 있을 때 아래와 같이 표기한다.

- 0,0 = AB = M0
- 0,1 = AB’ = M1
- 1,0 = A’B = M2
- 1,1 = A’B’ = M3

계산은 아래와 같이 한다.

- M1**•**M3 = (A+B’) **•** (A+B)

## 회로

논리 게이트 : 회로 상에서 boolean algebrea의 연산식을 구현한 것

인버터 : 회로상 input, 연산식에서의 변수를 의미함

직렬 스위치 : AND function

병렬 스위치 : OR function

조합회로 : 인버터, 게이트를 조합하여 구성한 회로

카르노 맵 : 부울 대수를 간소화하는 도구로 간단하고 직관적으로 논리식을 간소화할 수 있음.

### 회로 종류

순차회로 : 이전 상태를 고려하여 출력하는 회로(flip-flop, latch 등)

가산기 : 디지털 회로에서 숫자를 더하는 회로. 

반가산기 : 두개 비트를 더해 합과 캐리를 반환하는 회로

전가산기 : 캐리와 두개 비트를 더해 합과 캐리를 반환하는 회로
