# 파일 입출력

C를 사용해 파일을 읽고 생성할 수 있다.

## 텍스트 파일

### 파일 읽기

파일 포인터를 생성해 파일의 위치를 받는다. 이 때 파일을 읽을 것인지 쓸 것인지 등에 따라 파라미터를 다르게 준다. 읽기모드는 r이다.

```c
FILE* file = fopen(FILE_NAME, "r");
if (!file) {
	printf("파일을 열 수 없습니다.\n");
	return -1;
}
char line[100];
printf("\n[고객 목록]\n");
while (fgets(line, sizeof(line), file)) {
	printf("%s", line);
}
fclose(file);
```

사용이 끝나면 fclose를 해주어 메모리 누수를 방지한다.

### 파일 쓰기

파일은 새로 작성하는 방법이 있고 이어서 작성하는 방법이 있다. w는 새로 파일을 작성하는 파라미터이고 a는 존재하는 파일에 이어서 작성하는 파라미터이다.

```c
FILE* file = fopen(FILE_NAME, "w");
fprintf(file, "ID: %d, Name: %s, Balance: %.2lf\n", id, name, balance);
```

```c
FILE* file = fopen(FILE_NAME, "a");
if (!file) {
	printf("파일을 열 수 없습니다.\n");
	return -1;
}
Customer c;
printf("고객 ID: ");
scanf("%d", &c.id);
printf("고객 이름: ");
scanf("%s", c.name);
printf("잔액: ");
scanf("%lf", &c.balance);
fprintf(file, "ID: %d, Name: %s, Balance: %.2lf\n", c.id, c.name, c.balance);
fclose(file);
```

두번째 함수는 파일에 한줄을 추가한다. 그와 달리 첫번째 함수는 기존의 파일을 덮어쓰기 때문에 기존의 값이 모두 사라진다.

fseek, fwrite로 일부만 수정도 가능하지만 글자 수가 달라지면 깨지기 때문에 안정성 문제로 추천하지 않는다.

## 바이너리 파일

기존에 읽기, 쓰기에서 사용하는 파라미터 뒤에 바이너리를 의미하는 b를 붙인다.

```c
FILE* fp = fopen(FILE_NAME, "ab");
if (!fp) {
    printf("파일 열기 실패!\n");
    return;
}
Transaction t;
printf("고객 ID : ");
scanf("%d", &t.customerID);
printf("종목 코드 : ");
scanf("%s", t.stockCode);
printf("매수/매도 (B/S): ");
scanf(" %c", &t.type);
printf("거래 수량 : ");
scanf("%d", &t.quantity);
printf("체결가 : ");
scanf("%lf", &t.price);

fwrite(&t, sizeof(Transaction), 1, fp);
fclose(fp);
```

## JSON 파일

json 파일 형식에 맞춰 텍스트를 입력한 후 저장을 json으로 하면 된다.

```c
FILE* jsonFile = fopen(FILE_PATH, "w");
    if (jsonFile == NULL) {
        printf("JSON 파일을 생성할 수 없습니다.\n");
        return;
    }
    fprintf(jsonFile, "[\n");  // JSON 배열 시작
    rewind(fp); // 파일 처음으로 이동
    for (int i = 0; i < size; i++) {
        fread(&customer, sizeof(C), 1, fp);
        fprintf(jsonFile, "  {\n");
        fprintf(jsonFile, "    \"name\": \"%s\",\n", customer.name);
        fprintf(jsonFile, "    \"accountNo\": \"%s\",\n", customer.accountNo);
        fprintf(jsonFile, "    \"type\": \"%s\",\n", customer.type);
        fprintf(jsonFile, "    \"amount\": %.2f\n", customer.amount);
        if (i == size - 1)
            fprintf(jsonFile, "  }\n");  // 마지막 요소 (쉼표 없음)
        else
            fprintf(jsonFile, "  },\n"); // 쉼표 추가
    }
    fprintf(jsonFile, "]\n"); // JSON 배열 종료
    fclose(jsonFile);
    printf("고객 정보가 transactions.json 파일에 저장되었습니다.\n");
```
