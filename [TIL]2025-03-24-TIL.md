# 전처리기

컴파일 전에 소스 코드에 특정 작업을 수행하는 기능

컴파일 전에 작업을 수행하기 때문에 코드의 일부를 치환하는 것과 같음

실행 전에 수행되기 때문에 실행 도중 바뀌는 값의 영향을 받지 않음

## #include

헤더 파일을 포함하기 위한 전처리기

코드가 모듈화 되어 유지보수가 쉬워짐

```c
#include <stdio.h> // 표준 라이브러리 포함 방법
#include "add.h" // 사용자 정의 헤더 포함 방법
```

## #define

상수 및 함수를 간결하게 정의하는 방법

값을 그냥 치환하기 때문에 함수에서 변수에 괄호를 씌우지 않는다면 연산자 우선순위에 따라 예상하지 못했던 결과가 나올 수 있다.

컴파일 단계에서 적용되기 때문에 함수 호출에 따른 오버헤드가 발생하지 않는다.

```c
#define PI 3.141592 // 상수 정의
#define POW(X) ( (X) * (X) ) // 매크로 함수 정의
```

## #ifdef, #ifndef, #endif

ifdef : 특정 값이 정의 되었을 때 실행되도록 하는 전처리기

ifndef : 특정 값이 정의 되지 않았을 때 실행되도록 하는 전처리기

아래처럼 디버그 모드 ON/OFF를 위해 사용할 수 있다.

```c
#include <stdio.h>
#define DEBUG // 이 줄을 주석 처리하면 디버깅 로그가 출력되지 않음
void save_trades(const char *filename) {
 FILE *file = fopen(filename, "w");
 #ifdef DEBUG
 printf("[DEBUG] 파일을 열고 있습니다: %s\n", filename);
 #endif
 if (file == NULL) {
 printf("파일을 열 수 없습니다!\n");
 return;
 }
 fprintf(file, "거래 내역: 100,000원 입금\n");
 #ifdef DEBUG
 printf("[DEBUG] 파일에 데이터를 기록했습니다.\n");
 #endif
 fclose(file);
 #ifdef DEBUG
 printf("[DEBUG] 파일을 닫았습니다.\n");
 #endif
 printf("거래 내역이 저장되었습니다!\n");
}
int main() {
 save_trades("trades.txt");
 return 0;
}
```

## #if, #elif, #else, #endif

특정 값에 따라서 다른 동작을 하도록 정의하는 전처리기

```c
#define <stdio.h>

#define TEST 20

void main(){
	#if TEST == 20
		printf("TEST = 20\n");
	#elif TEST == 30
		printf("TEST = 30\n");
	#else
		printf("TEST는 20도 아니며 30도 아니다.\n");
	#endif
}
```

## #pragma

특정 컴파일러 설정 변경

#progma once가 있으면 한번의 #include 이후 파일을 다시 열고 읽지 않아 빌드 시간을 줄일 수 있다.

```c
#pragma once  // 소스 코드 파일을 컴파일할 때 컴파일러에 헤더 파일이 한 번만 포함되도록 지정합니다.
#pragma warning(disable: 4996) // 4996번 경고를 비활성화
void save_to_file(const char* filename, const char* content) {
 FILE *file = fopen(filename, "w"); // fopen() 사용 (보안 경고 발생 가능)
 if (!file) {
 printf("파일을 열 수 없습니다!\n");
 return;
 }
 fprintf(file, "%s", content);
 fclose(file);
 printf("파일 저장 완료: %s\n", filename);
}
int main() {
 char filename[100] = "test.txt";
 char content[256];
 printf("저장할 내용을 입력하세요: ");
 fgets(content, sizeof(content), stdin);
 save_to_file(filename, content);
 return 0;
}
```

# 모듈화

프로그램을 독립적인 기능을 수행하는 작은 단위로 나눠 개발하는 소프트웨어 설계 기법

유지보수성, 재사용성이 증가

## C언어에서 모듈화

- 헤더파일과 소스파일을 분리하여 모듈화
    - 헤더파일 : 함수 선언 및 변수 정의하는 부분
    - 소스파일 : 함수 실제 구현하는 부분
- 독립적인 코드 관리 기능
    - 필요할 때 필요한 모듈만 수정하면 됨
- 컴파일 시간 단축 및 협업 용이
    - 여러 개발자가 각각 모듈을 동시에 개발 가능

## 모듈화의 기준

1. 하나의 모듈은 한 가지 역할만 수행해야 한다.
2. 인터페이스와 구현 분리. 헤더파일과 소스파일로 분리한다.
3. 높은 응집도와 낮은 결합도. 관련 기능끼리 묶고 모듈 간 의존성을 최소화한다.
4. 재사용성 고려 - 공통 기능은 별도의 모듈로 분리
5. 변경 가능성 고려 - 확장 가능하도록 설계하여 유지보수를 쉽게 한다.

## static

전역 static 변수는 같은 이름의 변수를 다른 파일에서 중복 선언 가능

static인 경우 해당 파일 내부에서만 사용할 수 있고 다른 파일에서는 접근 불가

## 소스파일 분할규칙

- main은 되도록 다른 함수, 구조체, 변수를 포함하지 않는다.
- 각각의 역할별로 소스파일을 분할한다. 잘게 쪼갤수록 코드의 양은 늘어나지만 그만큼 관리는 쉬워진다.
- 대부분 다른 소스파일에서의 전역변수를 직접 끌어 쓰지 않는다.(extern으로 끌오지 않는다.)
- 각 소스파일은 가능한 한 독립적이게 만든다.
- 한 소스파일이 다른 소스파일에 의존적이게 되는 경우는 상관없지만 서로 의존되게 만들지는 않는다.
- 각 소스파일의 전역변수들은 특별한 경우가 아니라면 무조건 static을 걸어준다.
