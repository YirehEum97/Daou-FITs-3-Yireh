# 250206

# 객체지향

## method overriging

상속 받은 method의 내용을 재정의하는 것

제약사항

- method의 이름이 같아야 한다.
- return type이 같아야 한다.
- parameter 개수와 type이 같아야 한다.
- access modifier는 부모의 것보다 같거나 더 넓어야한다.
    - private < default < protected < public

```java
class A {
	public int sleep(int k){
	}
}

class B extends A {
	...
	@Override
	public int sleep(int k){
	}
}
```

코드 안정성을 위해서 어노테이션(@Override)을 붙여주는 것이 좋다.

## IS-A relationship

정의 : Sub class is a Super class

사각형 class의 sub class인 정사각형에 대해 ‘정사각형은 사각형이다.’ 라는 명제가 참인 것과 같다.

sub class type을 써야하는 자리에 super class의 type을 사용해도 된다.

## this, super, this(), super()

- `this` : this keyword가 사용된 instance에 대한 reference
- `super` : this와 같은 의미이지만 실제로 가리키는 객체는 상위 클래스의 객체
- `this()` : constructor 내에서 overloading된 다른 constructor를 호출하기 위해 사용
- `super()` : constructor 내에서 상위 class의 constructor를 호출하기 위해 사용

## abstraction

객체들의 공통적인 특징을 뽑아내 이름을 붙이는 것

## encapsulation

데이터 구조와 데이터를 다루는 방법을 결합시켜 묶은 것

class로부터 파생된 객체가 독립적으로 역할을 수행하기 위해 field와 method를 묶어 관리

## Information Hiding

class에서 외부에서 알 필요가 없는 fiel나 method를 외부에 노출될 필요가 없음

최소의 정보만 이용해서 사용할 수 있게 해야함

## final keyword

마지막이라는 것을 알려주기 위해 사용한다.

- class
class 앞에 붙을 경우 해당 class를 상속할 수 없다. class를 트리구조로 나타냈을 때 무조건 리프노드가 된다.
- field
field 앞에 붙을 경우 해당 변수는 변경할 수 없다. 즉 상수가 된다.
- method
method 앞에 붙을 경우 하위 class에서 overriding을 할 수 없다.

## abstract class

아직 완성되지 않고 개념적으로 만들어진 class

```java
public abstract class MyClass(){
	public abstract myFunc();
}

class SubClass extends MyClass(){
	@Override
	public myFunc(){
	}
}
```

위의 예제와 같이 myFunc는 method의 동작이 정의되지 않았다. 그저 이러한 method가 있다고 정의만 한 것이다. 이러한 class가 instance를 생성하는 경우 불완전한 method가 있기 때문에 **abstract class는 직접적으로 instance를 생성할 수 없다.** 하위 class에서 상속 받은 후 abstract method를 overriding 해 동작을 정의한 후 instance를 만들 수 있다.

## polymorphism & dynamic binding

부모의 객체 타입으로 다양한 형태의 자식 객체를 생성, 사용할 수 있는 것을 다형성이라고 부른다.

다형성을 이용해 상위 데이터 타입으로 객체의 타입을 통일하면 프로그램 처리가 쉬워진다.

```java
// 방법 1
public class PersonTest {
	
	public static void main(String[] args){
	
		Person person = new Person();
		Person student = new Student();
		Person teacher = new Teacher();
		
		person.eat();
		person.sleep();
		student.eat();
		...	
	}
}

// 방법 2
public class PersonTest {
	
	public static void main(String[] args){
	
		Person[] person = {
			new Person(),
			new Student(),
			new Teacher()
		};
		
		for(Person P : person) {
			p.eat();
			p.sleep();
		}
	}
}
```

Person을 상속 받은 Student, Teacher 클래스에 대해서 각자 instance를 하나씩 만들고 Person에 있는 method인 eat, sleep을 각각 실행하는 것은 같은 코드를 여러번 작성해야한다. 그러나 데이터 타입을 통일한다면 코드 작성 및 관리가 편해진다.

해당 함수를 각 class에 맞게 overriding한다면 같은 코드로 각각 class에 적합한 동작을 할 수 있다.

## interface

동일한 기능에 대해서는 같은 method 명을 사용하도록 강제

method명은 지정해주지만 내부 구현은 각자에 맞게 구현

```java
// 인터페이스 정의
public interface 인터페이스명 {
	public final static Data_type 상수명 = 상수값 ;
	public Return_Type 메소드명 ( 파라미터1, 파라미터2, ...);
}

// 인터페이스 사용
public class 클래스명 implements 인터페이스명 {
	// 인터페이스에 선언된 메소드 구현
}
```

특징

- interface 안의 모든 method는 abstract method 이다.
- interface 안의 모든 field는 public final static이 적용됨. 따라서 항상 interface의 모든 field는 상수이다.
- 인터페이스에서 선언된 모든 추상 method를 구현하면 일반적인 class가 되고 구현을 하지 않으면 abstract class가 된다.
- class 상속과 달리 여러 interface를 동시에 이어받을 수 있다.

# 주요 class

## Object class

### equals() method

== 은 각 객체의 주소 값을 비교하여 boolean 값을 반환한다. 객체의 주소 값이 아닌 객체의 데이터를 비교하기 위한 method가 필요하다. 이를 위해 모든 class의 상위 class인 Object class에 equals() method를 만들어 두 객체의 데이터를 비교할 수 있도록 한다.