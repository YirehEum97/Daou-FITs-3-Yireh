# 250210

# JDBC

Java로 Database를 활용하기 위한 API

### JDBC Driver

Java 응용 프로그램이 데이터베이스 상호작용할 수 있도록 하는 소프트웨어 구성 요소(class, interface)

DB마다 사용하는 방법이 다름. JDBC Driver에서 제공받은 interface를 이용해 각 언어별로 class를 만든다면 다양한 DB를 일관된 코드로 사용 가능.

## JDBC 연결 6단계

### 1. Driver 등록

어떤 제품의 데이터베이스를 사용할 것인지 확인

프로그램에서 딱 한번만 수행

try - catch문이 있어야 실행

```java
public class FirstConnect {
    public static void main(String[] args){
        try {
            Class.forName("oracle.jdbc.driver.OracleDriver");
            System.out.println("Driver loading Success");
        } catch (ClassNotFoundException c){
            System.out.println("Can not found Driver");
        }
    }
}
```

### 2. DBMS와 연결

만약 연결이 성공하면 연결 성공의 증표로 객체 생성

객체의 class는 java.sql.Connection class

연결을 하려면 3가지 정보가 필요

- 어떤 DB에 접속하는지에 대한 정보(문자열) → JDBC URL
    
    ```java
    // 예시
    String JDBC_URL = "jdbc:oracle:thin:@localhost:1521:xe";
    ```
    
- ID
- PW

### 3. Statement 생성

SQL 문장 만드는 과정

Statement 종류

- 일반 Statement
단순하며 기능이 적음
잘 안씀
- PreparedStatement
코드 작성하기 편함
효율적임
SQL Injection 방지
많이 씀.
- CallableStatement
Stored Procedure를 호출하기 위해 사용

### 4. SQL 전송

- excuteUpdate() : SQL 구문이 INSERT, UPDATE, DELETE 계열일 때 사용
- excuteQuery() : SQL 구문이 SELECT일 때 사용

### 5. SQL 결과 받기

- excuteUpdate() : SQL에 의해 영향 받은 행의 수가 int형으로 return 된다.
- excuteQuery() : 결과가 ResultSet으로 반환

### 6. 연결 닫기

ResultSet, Statement, Connection 자원을 사용했음

JAVA가 아닌 외부 DB를 사용했기 때문에 gc가 자동으로 종료하지 못함

따라서 사용이 끝났다면 호출한 자원을 역순으로 close() 메소드를 호출해야함.

## Transaction

작업의 최소 단위

특징

- 원자성(Atomicity) : 모든 작업이 완료되거나 단 하나도 되지 않음
- 일치성(Consistency) : 트랜잭션 변경 사항 적용 이후에도 데이터베이스의 제약이나 규칙을 만족
- 고립성(Isolation) : 모든 트랜잭션은 다른 트랜잭션으로부터 독립되어야함
- 지속성(Durability) : 하나의 트랜잭션이 성공적으로 수행되었다면 해당 트랜잭션에 대한 로그가 남아야하는 성질

### JDBC에서 Transaction 설정

Connection에 설정

Default로 autoCommit mode로 각 SQL이 하나의 트랜잭션이 됨

해당 모드를 끄고 다양한 쿼리를 하나의 트랜잭션으로 묶을 수 있음

```java
// transaction 시작
con.setAutoCommit(false);

...

// 롤백
con.rollback();

// 커밋
con.commit();
```