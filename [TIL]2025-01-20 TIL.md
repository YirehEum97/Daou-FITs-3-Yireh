# 250120

# 서브쿼리

SQL 문 안에 포함된 또 다른 SQL문

메인 쿼리에 값을 제공하거나 조건을 지정하는 역할

## 서브쿼리 분류

연관 여부에 따른 분류 : 비연관 서브쿼리, 연관 서브쿼리

위치에 따른 분류 : 스칼라 서브쿼리, 인라인 뷰, 중첩 서브쿼리

반환값 유형에 따른 분류 : 단일 행 서브쿼리, 다중행 서브쿼리, 다중열 서브쿼리

### 비연관 서브쿼리

서브쿼리가 독립적으로 실행되어 결과를 반환함

메인 쿼리와 서브쿼리 간 상호작용 없음

```sql
SELECT NAME
FROM EMPLOYEES
WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES);
```

### 연관 서브쿼리

서브쿼리가 메인 쿼리의 각 행마다 실행

메인 쿼리의 데이터를 서브쿼리에서 참조

```sql
SELECT NAME FROM EMPLOYEES E1
WHERE SALARY = (
	SELECT MAX(SALARY)
	FROM EMPLOYEES E2
	WHERE E1.DEPARTMENT_ID = E2.DEPARTMENT_ID);
```

### 스칼라 서브쿼리

서브쿼리가 컬럼 값으로 반환되어 SELECT 절에서 사용

주로 집계 값이나 계산 결과를 포함할 때 활용

```sql
SELECT NAME, SALARY,
	(SELECT AVG(SALARY)FROM EMPLOYEES
	WHERE DEPARTMENT_ID = E.DEPARTMENT_ID) AS AVG_SALARY)
FROM EMPLOYEES E;
```

### 인라인 뷰

FROM 절에서 서브쿼리 결과를 테이블처럼 사용

별칭을 붙여 활용 가능

```sql
SELECT D.DEPARTMENT_NAME, T.AVG_SALARY
FROM DEPARTMENTS D,
	(SELECT DEPARTMENT_ID, AVG(SALARY) AS AVG_SALARY
	FROM EMPLOYEES GROUP BY DEPARTMENT_ID) T
WHERE D.DEPARTMENT_ID = T.DEPARTMENT_ID;
```

### 중첩 서브쿼리

WEHRE 절에서 조건을 설정하기 위해 서브쿼리 사용

서브쿼리 결과에 따라 메인 쿼리의 필터링 결정

HAVING 절에서 조건을 설정하기 위해 집계 함수와 함께 활용

```sql
SELECT DEPARTMENT_ID, SUM(SALARY) AS TOTAL_SALARY
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
HAVING SUM(SALARY) > (SELECT AVG(SALARY)
								FROM EMPLOYEES
								GROUP BY DEPARTMENT_ID);
```

### 단일 행 서브쿼리, 다중 열 서브쿼리

서브쿼리가 단일 행(단일 열)을 반환

스칼라 서브쿼리가 포함됨

```sql
SELECT NAME
FROM EMPLOYEES
WHERE SALARY = (SELECT MAX(SALARY) FROM EMPLOYEES);
```

### 다중 행 서브쿼리

서브쿼리가 여러 행을 반환

다중 행 연산자와 함께 사용

```sql
SELECT NAME
FROM EMPLOYEES
WHERE DEPARTMENT_ID IN (SELECT DEPARTMENT_ID
									FROM DEPARTMENTS
									WHERE LOCATION = 'New York');
```

### 다중 열 서브쿼리

서브쿼리가 여러 열을 반환

메인 쿼리의 여러 열과 비교 가능

```sql
SELECT NAME
FROM EMPLOYEES
WHERE (DEPARTMENT_ID, JOB_ID) IN (
	SELECT DEPARTMENT_ID, JOB_ID
	FROM JOB_HISTORY);
```

### 서브쿼리 사용 시 유의사항

- 성능 최적화
    - 비연관 서브쿼리는 독립적으로 실행되므로 더 빠름
    - 연관 서브쿼리는 메인 쿼리의 각 행마다 실행되므로 성능 저하 가능
- 적절한 반환값 확인
    - 서브쿼리가 반환한느 값의 유형에 따라 연산자 선택이 중요
- JOIN 대체 가능성
    - 일부 서브쿼리는 JOIN으로 대체할 수 있어 성능 개선 가능

```sql
-- 가장 많은 잔액을 가진 계좌의 잔액과 계좌 ID를 조회
SELECT ACCOUNT_ID, BALANCE
FROM ACCOUNTS
WHERE BALANCE = (SELECT MAX(BALANCE) FROM ACCOUNTS);

-- 고객 중 가장 최근에 가입한 고객의 이름과 가입 날짜를 조회
SELECT NAME, CREATED_AT
FROM CUSTOMERS
WHERE CREATED_AT = (SELECT MAX(CREATED_AT) FROM CUSTOMERS);
```

# 집합연산자

SQL에서 두 개 이상의 SELECT 결과를 결합하여 하나의 결과 집합을 생성

## 집합연산자 종류

- UNION : 두 SELECT 결과를 합치되 중복 제거
    - UNION ALL 연산을 한 후 중복을 제거함. UNION ALL보다 오래 걸리므로 중복 결과가 없다는 확신이 있다면 UNION ALL을 써야함.
- UNION ALL : 두 SELECT 결과를 합치되 중복 포함
- INTERSECT : 두 SELECT 결과의 교집합(중복 제거)
- MINUS : 첫 번째 SELECT 결과에서 두 번째 SELECT 결과를 뺀 차집합(중복 제거)

### UNION ALL

- 두 SELECT 결과를 합치며 중복된 행도 포함
- 결과는 정렬되지 않음

### UNION

- 두 SELECT 결과를 합친 뒤 중복된 행을 제거
- 결과는 정렬된 상태로 반환(최적화 과정에서 낮은 확률로 SORTING이 안됨)
- UNION ALL 이후 정렬, 중복 제거의 과정을 거치기 때문에 UNION ALL보다 많은 연산을 함
- ORDER BY는 아래에 있는 테이블에만 사용할 수 있음
- 결과물은 첫 번째 테이블의 ALIAS를 따름

### INTERSECT

- 두 SELECT 결과의 교집합 반환
- 중복된 행은 하나만 반환

### MINUS

- 첫번째 SELECT 결과에서 두 번째 SELECT 결과를 제외한 차집합 반환
- 중복된 행은 하나만 반환

## 집합연산자 사용 시 유의사항

- SELECT 절에 나열된 컬럼 수와 데이터 타입이 동일해야 함
- 결과 집합의 순서는 기본적으로 보장되지 않음
- ORDER BY는 최종 결과에만 적용 가능
- 대량의 데이터 처리 시 성능에 유의해야 함
- ALIAS는 맨 처음 SELECT 절을 따름

### 예제

```sql
-- CUSTOEMRS와 LOANS에 공통적으로 존재하는 고객 ID 조회
SELECT CUSTOMER_ID FROM CUSTOMERS
INTERSECT
SELECT CUSTOMER_ID FROM LOANS;
```

```sql
-- TRANSACTIONS에서 거래 유형과 ACCOUNTS의 계좌 유형을 중복 없이 결합하여 조회
SELECT TRANSACTION_TYPE FROM TRANSACTIONS
UNION
SELECT ACCOUNT_TYPE FROM ACCOUNTS;
```

# 윈도우 함수

 SQL에서 행을 기준으로 특정 범위를 정의하여 작업하는 함수

*GROUP BY와 달리 축약되어 나오지 않음. 집계를 한다면 해당 값만 나오는 것이 아니라 모든 행에 해당 값이 붙어서 나옴

## 주요 용도

- 순위 계산
- 이동 평균
- 누적 합계
- 그룹별 최대값/최소값

### 순위 함수

RANK : 순위를 계산하여 동일한 값은 같은 순위를 부여(동일 순위 이후는 순위를 건너뜀)

DENSE_RANK : 순위를 계산하여 동일한 값은 같은 순위를 부여

ROW_NUMBER : 순위를 고유하게 매김

```sql
SELECT 컬럼명,
	RANK() OVER(PARTITION BY 컬럼 ORDER BY 컬럼) AS rank,
	DENSE_RANK() OVER(PARTITION BY 컬럼 ORDER BY 컬럼) AS dense_rank,
	ROW_NUMBER() OVER(PARTITION BY 컬럼 ORDER BY 컬럼) AS row_number
FROM 테이블명;
```

```sql
-- EMPLOYEES 테이블에서 급여 순으로 순위를 계산
SELECT EMPLOYEE_ID, NAME, SALARY,
	RANK() OVER(ORDER BY SALARY DESC) AS rank
FROM EMPLOYEES;
```

## 윈도우 집계 함수의 기본 문법

```sql
SELECT 컬럼명,
	집계함수() OVER(PARTITION BY 컬럼
							ORDER BY 컬럼) AS 결과컬럼
FROM 테이블명;
```

OVER 안에서 ORDER BY를 사용하면 누적치에 대한 결과물을 반환한다. 즉 SUM(ORDER BY IMPLOYEE_ID)라는 식으로 사용한다면 직원번호 순서대로 나열하고 순서대로 누적합을 계산하여 표시한다.

```sql
-- 부서별 급여 합계 계산
SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
    SUM(SALARY) OVER
    (PARTITION BY DEPARTMENT_ID) AS DEPT_TOTAL
FROM EMPLOYEES;

-- 부서별 급여 누적 합계 계산
SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
    SUM(SALARY) OVER
    (PARTITION BY DEPARTMENT_ID ORDER BY SALARY) AS DEPT_TOTAL
FROM EMPLOYEES;
```

### PARTION BY, ORDER BY

- PARTITION BY
    - 데이터를 특정 그룹으로 나눔
    - 그룹별로 윈도우 함수가 작동
- ORDER BY
    - 데이터를 정렬하여 윈도우 함수의 동작 순서를 지정
    - 누적 계산에 주로 사용

## 윈도우 함수 옵션

- 윈도우 함수에서 특정 행이나 범위를 지정하는 추가 설정
- 옵션을 활용해 데이터 처리 범위를 유연하게 조정 가능
- 주요 옵션
    - ROWS
    - RANGE
    - UNBOUNDED
    - CURRENT ROW
    - PRECEDING / FOLLOWING

## 기타

ROWS는 다중조건이 가능하다. 아래와 같은 코드는 SALARY, EMPLOYEE_ID 기준으로 정렬을 한 후 인접한 데이터와 함께 합을 계산하여 반환한다.

```sql
SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
    SUM(SALARY) OVER
    (PARTITION BY DEPARTMENT_ID ORDER BY SALARY, EMPLOYEE_ID
    ROWS BETWEEN 1 preceding and 1 following) AS DEPT_TOTAL
FROM EMPLOYEES;
```

RANGE는 다중조건이 불가능하다. 아래와 같이 작성해 SALARY, EMPLOYEE_ID가 모두 1만큼 거리 안에 있는 데이터에 대한 연산을 진행하려 했다. 이것이 가능했다면 제한적으로 vectorDB 처럼 쓸 수 있을텐데 아쉽다.

```sql
-- 에러 발생
SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
    SUM(SALARY) OVER
    (PARTITION BY DEPARTMENT_ID ORDER BY SALARY, EMPLOYEE_ID
    RANGE BETWEEN 1 preceding and 1 following) AS DEPT_TOTAL
FROM EMPLOYEES;
```