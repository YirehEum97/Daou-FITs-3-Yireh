# 250214

# 인터넷

전세계 네트워크를 서로 연결시킨 통신망을 의미한다.

## 네트워크

컴퓨터들이 통신 기술을 이용해 서로 연결되어있는 통신 형태

### 논리 주소

IP 주소. 특정 규칙에 따라 할당되는 값이지만 변경이 가능하다.

### 물리 주소

MAC 주소. 장치를 만들 때 정해지는 값. 전세계에서 중복되는 값이 없도록 세계적인 협약으로 관리하고 있다.

### PORT

네트워크에 연결된 컴퓨터에서 여러 프로그램이 통신을 할 때 혼선이 생기지 않도록 도와주는 번호. 각 프로그램은 본인의 port 번호를 이용해 통신을 한다.

### HTTP

HyperText Transfer Protocol

web에서 정보를 주고 받기 위해 사용하는 프로토콜

### Web Service

- Static Web
    - html
    - 클라이언트는 서버에 resource를 요청함
- Dynamic Web
    - WAS(apache, NginX)
    - 클라이언트는 서버에 resource 뿐만 아니라 프로그램의 실행 결과도 요청함

# Tomcat

apache 재단에서 개발하는 JAVA기반 servlet container.

API를 추가해 웹 어플리케이션 서버 TomeEE로 사용하기도 한다.

WAS는 client의 request를 받아 내부에서 연산한 후 결과를 response로 반환한다.

### 구동 순서

1. web client가 web server에 Get혹은 Post 방식으로 request를 날린다.
2. web server는 WAS에 문자열 형태로 request를 전달한다.
3. WAS는 해당 request를 처리하기 위한 class의 객체(thread)가 있는지 검색한다.
4. 있다면 해당 객체를 사용하고 없다면 새로운 객체를 생성한다.
5. 일반적으로 하나의 request는 하나의 thread에서 처리된다.

추가적으로 thread를 생성하는 것은 비용이 크기 때문에 jdbc에서 connection을 connection pool에서 관리하며 하나씩 빌려주는 것처럼 thread pool에서 가지고 있는 thread를 request에 할당하는 방식으로 관리한다.

### GET, POST

GET은 사용자의 입력 값을 url에 붙여 전달한다. 따라서 외부에서 사용자가 넘긴 파라미터를 파악하기 쉬워 보안이 필요하지 않은 값을 전달할 때 사용한다.

POST는 사용자의 입력 값을 html 파일을 사용해 전달한다.

Tomcat에서는 doGet, doPost 메소드가 있어 request의 방식에 따라 자동으로 메소드를 호출한다.