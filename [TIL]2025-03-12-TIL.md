## 메모리 구조

### 코드 영역

함수, 명령어 읽기 전용

### 데이터 영역

전역변수, 정적변수

### 스택 영역

지역변수, 매개변수, 리턴

### 힙 영역

사용자 메모리 공간

## 포인터에 저장되는 값

포인터에는 주소값만 저장된다.

그러면 어차피 OS에 따라 고정된 크기의 주소만 저장되는데 int의 포인터인지 char의 포인터인지가 의미가 있나 의문이 들었다.

실제로 int의 포인터의 주소값을 char**에 저장했더니 출력했을 때 값이 달라지는 문제가 발생했다.

이는 포인터의 형에 따라 최종적으로 몇 byte의 정보를 읽어올지가 정해져 생기는 문제였다.

```c
void main() {
	char c = 'A';
	char* p = &c;
	char** pp = &p;
	short* sp = &c;
	short** spp = &sp;

	printf("c: %c\n", c);
	printf("p: %c\n", *p);
	printf("pp: %c\n", **pp);
	printf("sp: %c\n", *sp);
	printf("spp: %c\n", **spp);
}
// 모두 A가 출력

```

위의 코드처럼 사이즈가 같은 두가지 형 char와 short에 대해 포인터 형변환 없이 사용했을 때 아무런 문제 없이 실행됨을 확인할 수 있다.

## 함수 포인터

함수를 가리키는 포인터

```c
int max(int, int);
int min(int,int);

int main(){
	int (*fpmm)(int, int);
	int num1,num2, flag;
	scanf("%d %d",&num1, &num2);
	printf("구하려는 것은 1.최대값  2.최소값\n");
	scanf("%d",flag);
	if (flag == 1){
		fpmm = max;
	}
	else {
		fpmm = min;
	}
	printf(fpmm(num1, num2));
	
	reutunr 0;
}
```

함수를 가리키는 포인터를 설정하고 해당 포인터가 어느 함수를 가리킬지 지정해 사용할 수 있다.

## 이중포인터

포인터 연습을 위해 아래와 같은 코드를 작성했다. 1번과 2번 코드중 어느 코드가 좋은 코드일까?

```c
	// 1번
	printf("\n대문자로 변환된 문자열:\n");
	for (int i = 0; i < 3; i++) {
		for (int j = 0; j < length[i]; j++) {
			if (str[i][j] >= 'a' && str[i][j] <= 'z') {
				str[i][j] -= 32;
			}
			printf("%c", str[i][j]);
		}
		printf("\n");
	}
	
	
	// 2번
		printf("\n대문자로 변환된 문자열:\n");
	for (int i = 0; i < 3; i++) {
		for (int j = 0; j < length[i]; j++) {
			if (*(*(str + i) + j) >= 'a' && str[i][j] <= 'z') {
				*(*(str + i) + j) -= 32;
			}
			printf("%c", *(*(str + i) + j));
		}
		printf("\n");
	}
```

이중 포인터를 사용하기 위해 2번의 코드를 작성했지만 작성도 불편하고 가독성이 많이 떨어진다.

성능의 차이가 없고 가독성, 작성의 편의성 등에서 모두 2번이 불편하기 때문에 1번이 더 좋은 코드다.

다만 아래의 예제에서는 이중포인터를 사용하는 것이 더 이득이다.

```c
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

void to_uppercase(char **str, int rows) {
    printf("\n대문자로 변환된 문자열:\n");
    for (int i = 0; i < rows; i++) {
        for (int j = 0; str[i][j] != '\0'; j++) {
            if (str[i][j] >= 'a' && str[i][j] <= 'z') {
                str[i][j] = str[i][j] - 32;
            }
        }
        printf("%s\n", str[i]);
    }
}

int main() {
    int rows = 3;

    // 문자열 배열을 동적 할당
    char **str = (char **)malloc(rows * sizeof(char *));
    str[0] = (char *)malloc(20 * sizeof(char));
    str[1] = (char *)malloc(20 * sizeof(char));
    str[2] = (char *)malloc(20 * sizeof(char));

    // 문자열 입력
    snprintf(str[0], 20, "hello");
    snprintf(str[1], 20, "world");
    snprintf(str[2], 20, "c programming");

    // 대문자로 변환
    to_uppercase(str, rows);

    // 메모리 해제
    for (int i = 0; i < rows; i++) {
        free(str[i]);
    }
    free(str);

    return 0;
}

```

uppercase에서는 인덱스로 접근하지만 main에서 str을 char** 로 선언하여 각 문자열의 크기를 다르게 할당할 수 있다.
