# 250310

## 매크로 함수

#define으로 전처리기 단에서 처리할 수 있는 함수이다. 아래 예제를 보면 두개의 함수 결과가 다르다. 이유는 x에 num + 2가 그대로 넘어가면서 power1의 식을 풀어쓰면 num +2 * num + 2가 되기 때문이다. 따라서 괄호를 사용해서 예상하지 못한 결과를 피하

```c
#include <stdio.h>
#define power1(x) ( x * x )
#define power2(x) ( (x) * (x) )
int main() {
 int num;
 printf(“정수를 입력하세요 : ”);
 scanf_s(“%d”, &num);  // 5를 입력
 printf(“%d의 제곱 : %d\n”, num, power1(num) ); // 25
 printf(“%d의 제곱 : %d\n”, num, power2(num) ); // 25
 printf(“(%d+2)의 제곱 : %d\n”, num, power1(num+2) ); // 17
 printf(“%d의 제곱 : %d\n”, num, power2(num+2) ); // 25
}
```

## 포인터 크기

포인터의 크기는 os에 따라 나뉜다. 64bit 운영체제에서는 8byte 크기를 차지한다.

## 한글 출력

일반적으로 char는 1byte지만 한글은 2byte의 크기가 필요하다. 따라서 메모리 공간에서 두칸에 저장이 된다. 따라서 한글을 제대로 출력하기 위해서는 아래와 같이 2칸을 출력해야한다.

```c
char s[] = "오";
printf("%c",s[0]); //글자를 인식하지 못해 ?로 출력
printf("%c%c",s[0],s[1]); //제대로 출력
```

## 문자열 라이브러리

- <string.h>
    - strlen() 문자열의 길이
- <ctype.h>
    - tolower() 소문자
    - toupper() 대문자
