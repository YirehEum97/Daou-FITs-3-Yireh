# 250114

# 데이터 정규화

데이터베이스의 설계 단계에서 데이터 중복을 제거하고 데이터 무결성을 유지하기 위한 과정

데이터를 논리적으로 분리하여 효율적으로 저장하고 관리하도록 설계

## 제1정규형

- 모든 속성은 반드시 하나의 값만 가져야 한다
하나의 행에 여러 데이터가 들어가지 않고 각각의 행으로 분리한다.
- 유사한 속성이 반복되는 경우도 1차 정규화의 대상
직업1, 직업2, 직업3과 같이 유사한 속성을 가진 속성은 하나의 속성으로 합친다.

## 제2정규형

- 엔터티의 모든 일반속성은 반드시 모든 주식별자에 종속되어야 한다.
주식별자가 두개 이상인 경우 일반 속성이 하나의 식별자에만 종속되는 경우는 테이블을 분리하여 정규화한다.

## 제3정규형

- 주식별자가 아닌 모든 속성 간에는 서로 종속될 수 없다.
예를 들어 일련번호(pk), 이름, 소속사코드, 소속사명이 있는 경우 소속사명은 소속사코드에 종속이 되기 때문에 테이블을 분리한다.

## 반정규화

- 데이터베이스 설계에서 정규화를 통해 분리된 테이블을 특정 성능 향상이나 시스템 요구에 따라 다시 결합하거나 중복 데이터를 허용하는 과정
- 성능 최적화와 효율적인 데이터 액세스를 위해 데이터 모델을 조정하는 것

### 반정규화의 필요성

- 정규화의 한계
정규화로 중복을 줄일 수 있지만 쿼리에서 다수 테이블 조인이 필요해서 성능 저하가 발생할 수 있다. 읽기 속도가 중요한 상황에서 정규화된 데이터 모델은 비효율적일 수 있다.
- 성능 최적화
쿼리 성능을 개선하기 위해 데이터를 중복 저장하거나 테이블을 합치는 방식으로 읽기 속도를 높임

### 반정규화 적용 방법

- 중복 데이터 저장 : 데이터 분석 속도를 위해 요약 데이터를 미리 계산하고 테이블에 저장
ex. 월별 매출 집계 데이터를 MONTHLY_SALES 테이블로 별도 관리
- 이력 데이터 관리 : 트랜잭션 데이터 외에도 과거 데이터를 효율적으로 조회하기 위해 반정규화 적용
ex. PRODUCTS 테이블에 최신 가격과 함께 과거 가격 정보를 중복 저장

### 반정규화의 장점

- 성능 개선
읽기 속도 개선, 조인 연산 감소
- 간단한 쿼리
데이터 구조가 간단해져 쿼리가 간단해짐
가독성이 좋아짐
- 특정 요구사항 대응

### 반정규화의 단점

- 데이터 중복
중복으로 인한 저장 공간 증가
데이터 동기화에서 복잡성 증가
- 데이터 무결성 문제
업데이트나 삭제 시 데이터 불일치 가능성 높아짐
- 유연성 감소
테이블 병합이나 속성 추가로 데이터 구조가 고정되어 유연성이 저하됨

## OLTP, OLAP

### OLTP

실시간으로 다수의 트랜잭션을 처리하는 시스템으로 주로 운영환경에서 사용

목적 : 데이터의 입력, 저장, 수정 및 조회를 빠르게 처리

트랜잭션 4가지 속성

- 원자성 : 일부만 성공하는 경우가 없어야함
- 일관성 : 트랜잭션 이전과 이후, 데이터베이스의 제약이나 규칙이 유지되어야함
- 고립성 : 각 트랜잭션은 서로 독립적임
- 지속성 : 트랜잭션 수행 이후 해당 로그가 남아야함

### OLAP

대규모 데이터를 분석하고 요약 정보와 통찰력을 제공하는 시스템

목적 : 데이터의 분석, 보고서 생성, 비즈니스 인사이트 제공

주요 특징

- 복잡한 분석 : 집계, 요약, 패턴 분석 쿼리를 통해 비즈니스 인사이트 제공
- 다차원 데이터 분석 : 시간, 지역, 제품 등 여러 차원을 기준으로 데이터 요약

구성요소

- 데이터 웨어하우스
- OLAP 서버
- 사용자 인터페이스

### 상호작용

OLTP 시스템은 데이터 생성, OLAP 시스템은 데이터 활용

ETL 프로세스 : 데이터 웨어하우스로 데이터를 전송하기 위해 ETL(추출, 전송, 로드) 사용

## 논리 모델, 물리 모델

### 논리모델

데이터베이스 설계의 한 단계로 데이터의 구조와 관계를 기술하는 모델

특정 DBMS에 종속되지 않으며 데이터의 논리적 구조를 나타냄

엔터티, 속성 정의

관계와 제약 조건 표현

정규화 적용

### 물리 모델

데이터베이스 설계의 최종 단계로 특정 DBMS에 종속적인 데이터 구조를 정의하는 모델

데이터의 저장소, 성능, 보안을 고려하여 실제 구현

데이터 타입과 제약 조건 지정

인덱스, 파티션, 저장소 구조 설계

DBMS의 기술적 제약 및 성능 최적화 반영

## 트랜잭션

데이터베이스에서 하나의 논리적 작업 단위를 의미

일련의 작업이 모두 성공하거나 모두 실패하는 것을 보장

데이터 무결성과 일관성을 유지하기 위한 기본 단위로 사용

### 원자성(Atomicity)

트랜잭션은 모두 실행되거나 모두 실행되지 않아야 함

실패 시 모든 변경 사항을 롤백하여 원래 상태로 복구

### 일관성(Consistency)

트랜잭션이 완료된 후 데이터베이스는 항상 일관된 상태를 유지해야 함

데이터 무결성을 보장

### 고립성(Isolation)

여러 트랜잭션이 동시에 실행될 때 서로 간섭하지 않도록 독립적으로 처리

트랜잭션 격리 수준으로 구현

READ UNCOMMITTED, READ COMMITTED 등이 있다.

### 지속성(Durability)

트랜잭션이 성공적으로 완료되면 변경된 데이터는 영구적으로 저장

시스템 장애 후에도 데이터는 유지

### 유의사항

트랜잭션  범위를 좁게 설계하여 성능과 오류 가능성 최소화

필요 이상의 데이터를 수정하거나 삭제하지 않도록 제한

트랜잭션이 너무 오래 실행되지 않도록 설계하여 데이터베이스 Lock 문제 방지

시스템 성능과 데이터 정확성 간의 균형을 유지

# SQL

Structured Query Language

데이터베이스를 관리하고 조작하기 위한 언어

데이터를 검색, 추가, 수정, 삭제하는데 사용

데이터 정의, 조작, 제어를 위한 명령어 집합 제공

### SQL 분류

- DDL
데이터베이스 구조 정의
    - CREATE : 테이블, 뷰, 인덱스 생성
    부족하지 않으면서 너무 과하지 않은 크기를 생각해서 할당
    NOT NULL은 에러가 발생할 수 있어서 꼭 중요한 경우가 아니면 잘 쓰지 않는다
    - ALTER : 기존 객체 수정
    컬럼 추가, 컬럼명 변경, 컬럼 삭제 등등
    - DROP : 객체 삭제 ← 혹시 해당 객체를 참조하는 테이블, 사용하는 함수가 있는지 찾아보고 조심히 사용해야함
    - TRUNCATE : 테이블 데이터 초기화. 테이블은 유지하되 데이터만 삭제
- DML
데이터를 조작. UPDATE, DELETE 등은 하고나서 SELECT로 원하는 결과가 나왔는지 확인. 모두 로그가 남아 롤백이 가능하다.
    - SELECT : 데이터 검색
    - INSERT : 데이터 삽입
    - UPDATE : 데이터 수정
    - DELETE : 데이터 삭제
- DCL
데이터 접근 권한 제어
    - GRANT : 권한 부여
    - REVOKE : 권한 회수
- TCL
트랜잭션 관리를 위한 명령어 제공
    - COMMIT : 트랜잭션 확정
    - ROLLBACK : 트랜잭션 취소
    - SAVEPOINT : 특정 지점 저장

### ALIAS

SQL 쿼리에서 컬럼이나 테이블에 부여하는 임시 이름

- 사용법
SELECT AS 별칭; ← AS는 생략 가능
SELECT 컬럼 FROM 테이블 별칭 ← 이 경우 테이블을 무조건 별칭으로 불러야함
특수문자, 공백, 한글 등을 포함하고 싶으면 쌍따옴표로 표시함
- 사용 이유
테이블 이름이 길거나 복잡할 때 간결한 이름으로 대체
JOIN 쿼리에서 테이블 이름을 축약하여 사용
- 예시

```sql
SELECT E.NAME AS EMPLOYEE_NAME,
	D.DEPARTMENT_NAME AS DEPT_NAME
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### NULL

데이터베이스에서 NULL은 값이 없음을 의미함

0, “”(빈 문자열)과는 다름

NULL 연산

- NULL 값은 다른 값과 비교할 수 없음
- 비교연산을 수행하면 항상 FALSE를 반환
- NULL 값을 검색할 때에는 WHERE 절에서 IS NULL로 검색

### WHERE

WHERE절로 조건을 지정하여 데이터를 필터링

SELECT, UPDATE, DELETE와 함께 사용

비교 연산자, 논리 연산자, 특수 연산자를 활용해 복잡한 조건 설정 가능

LIKE

특정 패턴을 가진 데이터를 검색

% : 0개 이상의 문자를 대체

_ : 1개의 문자를 대체

## 기타

- 파생변수(파생속성)
자주 필요한 속성의 통계값 등을 저장해서 필요할 때 계산하는 과정을 생략할 수 있도록 하는 변수