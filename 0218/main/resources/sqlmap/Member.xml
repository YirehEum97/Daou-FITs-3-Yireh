<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
    "-//mybatis.org/DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="Member">
    <select id="tryLogIn" parameterType="HashMap"
        resultType="String">
        <![CDATA[
            SELECT NAME
            FROM MEMBERTBL
            WHERE ID =  #{id}
                AND PASSWORD = #{pw}
        ]]>
    </select>
</mapper>