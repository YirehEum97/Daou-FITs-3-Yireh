# 250117

# SQL 실습

### 최근 6개월 거래 내역 조회

최근 6개월 동안 발생한 거래의 계좌 ID와 거래 금액, 거래일, 고객 ID, 고객명을 조회하세요. (단, 거래일은 년-월-일 시:분:초 형식으로 출력)

```sql
SELECT T.ACCOUNT_ID, 
       T.AMOUNT, 
       T.TRANSACTION_DATE TRAN_DATE,
       C.CUSTOMER_ID,
       C.NAME
FROM TRANSACTIONS T, ACCOUNTS A, CUSTOMERS C
WHERE T.ACCOUNT_ID = A.ACCOUNT_ID
AND A.CUSTOMER_ID = C.CUSTOMER_ID
AND MONTHS_BETWEEN(SYSDATE, T.TRANSACTION_DATE)<= 6
;
```

처음에는 이렇게 풀었는데 내가 설정을 바꿔서 DATE가 자동으로 초까지 나오는거라 일반적으로 저 형식을 맞추기 위해서 문자형으로 변환하는게 좋다.

```sql
SELECT T.ACCOUNT_ID, 
       T.AMOUNT, 
       TO_CHAR(T.TRANSACTION_DATE, 'YYYY-MM-DD HH24:MI:SS') AS TRAN_DATE,
       C.CUSTOMER_ID,
       C.NAME
FROM TRANSACTIONS T, ACCOUNTS A, CUSTOMERS C
WHERE T.ACCOUNT_ID = A.ACCOUNT_ID
AND A.CUSTOMER_ID = C.CUSTOMER_ID
AND T.TRANSACTION_DATE >= ADD_MONTHS(SYSDATE, -6)
;
```

### 각 계좌별 거래 횟수

각 계좌의 거래 횟수를 조회하고 거래 횟수를 오름차순으로 출력하세요

```sql
SELECT A.ACCOUNT_ID, 
       COUNT(T.TRANSACTION_ID) AS TRANSACTION_COUNT
FROM ACCOUNTS A
LEFT OUTER JOIN TRANSACTIONS T
ON A.ACCOUNT_ID = T.ACCOUNT_ID
GROUP BY A.ACCOUNT_ID
ORDER BY TRANSACTION_COUNT;
```

계좌 테이블에 거래 테이블을 붙여 각 계좌에서 어떤 거래가 있었는지 볼 수 있게 만들었다. 이후 각 계좌별로 GROUP 후 COUNT를 해주면 되는데 TRANSACTIONS 테이블에만 있는 속성이 아닌 다른 속성 혹은 *을 COUNT한다면 거래 내역이 없는 계좌도 1이 카운트가 되는 문제가 발생한다.

### 부서별 직원 수 확인

급여가 7000이상 15000이하인 직원 데이터를 기준으로 각 부서별 직원수를 계산하세요. 직원 수가 5명 이상인 부서만 출력하고, 직원 수가 많은 순으로 정렬하세요.

```sql
SELECT C
FROM (
    SELECT COUNT(*) C
    FROM EMPLOYEES E
    WHERE E.SALARY BETWEEN 7e3 AND 1.5e4
    GROUP BY department_id
    ORDER BY C DESC )
WHERE
    C >= 5
```

처음에 이렇게 풀었다. 직원수 제한을 안보고 코드를 짰다가 급하게 추가하느라 생각 안하고 적어서 급하게 추가하느라 이렇게 됐다. 사실 HAVING절만 추가하면 간단하게 해결되는 문제였다.

```sql
SELECT COUNT(*) C
FROM EMPLOYEES E
WHERE E.SALARY BETWEEN 7e3 AND 1.5e4
GROUP BY department_id
HAVING COUNT(*) >= 5
ORDER BY C DESC
```